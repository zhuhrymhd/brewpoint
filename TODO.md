# BrewPoint — MVP Build Checklist (Frontend-First)

**Purpose:** a step-by-step, checkable to-do list covering the entire MVP end-to-end. Use this as your personal reference while building — check items off as you go.

**Stack note:** BrewPoint MVP is built as **one Next.js app** — Route Handlers as the API layer, Drizzle ORM, PostgreSQL. There is no separate Go service for MVP (see `TECH_SPEC.md` v2.0 and Section 12 for the optional future split).

**Order:** Frontend UI first (builds a clickable template with mock data) → Backend logic (Route Handlers + Drizzle + database, added into the _same_ app) → Integration (swap mocks for real calls) → Launch.

Refer back to `PRD.md` for feature detail, `TECH_SPEC.md` for schema/API contracts, and `DESIGN_SYSTEM.md` for exact tokens.

---

## Part 1 — Frontend (UI Template, Mock Data)

Goal of this part: a fully clickable Next.js app with every screen built, using fake/hardcoded data — the database and API routes don't exist yet. This becomes your visual template and forces you to think through every UI state before wiring real data in.

### 1.1 Project Setup

- [x] `create-next-app` with TypeScript, App Router, Tailwind CSS — this single app (`brewpoint`) will hold both UI and API
- [x] Install and init shadcn/ui (`npx shadcn init`)
- [x] Set up folder structure per `TECH_SPEC.md` Section 2 (`app/`, `components/`, `lib/`, `stores/`, `hooks/`) — you can leave `app/api/`, `lib/db/`, and `lib/services/` empty for now, they get filled in Part 2
- [x] Install fonts: Poppins + Inter via `next/font/google` (`app/fonts.ts`)
- [x] Wire `--font-display` (Poppins) and `--font-body` (Inter) into Tailwind — done via `app/globals.css` `@theme inline` block (project uses Tailwind v4, no `tailwind.config.ts`)
- [x] Paste `DESIGN_SYSTEM.md` Section 2.5 CSS variables into `app/globals.css`
- [x] Add `--success`/`--warning` custom tokens on top of shadcn's default theme variables
- [x] Verify base layout renders with the correct fonts and background color before building anything else

### 1.2 Atomic / Reusable Components

Install shadcn primitives first, then build BrewPoint-specific compositions on top:

- [x] `npx shadcn add button` — verify `primary`/`secondary`/`outline`/`destructive`/`ghost` variants match `DESIGN_SYSTEM.md` 6.1
- [x] `npx shadcn add input`, `label`, `textarea`
- [x] `npx shadcn add select`, `dropdown-menu`
- [x] `npx shadcn add dialog` (modals), `sheet` (side drawers)
- [x] `npx shadcn add table`
- [x] `npx shadcn add card`
- [x] `npx shadcn add badge` — theme it for status tags (completed/voided/low-stock/out-of-stock/active/inactive) per 6.3
- [x] ~~`npx shadcn add form`~~ — shadcn no longer ships a single `form` component (confirmed via docs, 2026-07); used `npm install react-hook-form @hookform/resolvers zod` + `npx shadcn add field` instead, and the `<Controller>` + `<Field>`/`<FieldLabel>`/`<FieldError>` pattern in place of the old `<Form>`/`<FormField>`
- [x] `npx shadcn add tabs` — for category filter chips on POS screen
- [x] `npx shadcn add skeleton` — loading states
- [x] `npx shadcn add sonner` or `toast` — success/error notifications
- [x] Build custom `<ProductTile />` — used in POS grid, `radius-xl`, touch-friendly
- [x] Build custom `<StatusBadge status="..." />` — wraps `Badge`, maps status string → correct color per 6.3
- [x] Build custom `<NumericValue />` — wraps text with `tabular-nums`, used for all prices/stock counts
- [x] Build custom `<EmptyState title icon action />` — reusable across every list screen
- [x] Build custom `<PageHeader title action />` — consistent page title (Poppins) + primary action button pattern

### 1.3 Layout Shell

- [x] Root layout (`app/layout.tsx`) — fonts, global styles
- [x] `(staff)/layout.tsx` — sidebar/nav shell wrapping all logged-in pages
- [x] Build nav with role-aware items: mock a `currentUser` object with a hardcoded `role` for now (swap for the real session in Part 3)
- [x] Nav items: POS, Products, Categories, Transactions, Dashboard (admin), Users (admin)
- [x] Build sidebar brand header (logo tile + shop name) and account widget footer (avatar/initials, name, role, logout button) — added 2026-07 per `docs/references/sidebar.html`, not in the original PRD-derived checklist; logout button is UI-only for now, wired to a real `POST /api/v1/auth/logout` call in Part 3.2

### 1.4 Screens — build each with hardcoded/mock data, covering all states from `DESIGN_PROMPT.md`

**Order note (2026-07):** re-sequenced easiest → hardest per user preference, instead of the original PRD feature order. Login was already in progress so it stays first; everything after is ordered by build complexity, ending with Sales Dashboard (needs `recharts` + date-range aggregation — the least-familiar territory) and POS/Checkout (most interaction states/Zustand wiring) near the end.

**Refactor convention (2026-07):** each screen starts as one working `page.tsx` file (fastest way to get the logic right), but must be split before moving to the next screen — otherwise `page.tsx` balloons into a god file. Standard split, established on Products: `lib/types.ts` (shared types), `lib/validators/{module}.ts` (zod schema), `lib/mock-{module}.ts` (mock data), `components/{module}/*.tsx` (each dialog/sheet/table as its own component, receiving data + callbacks as props). `page.tsx` should end up as a thin orchestrator — state + wiring only, no large inline JSX blocks. Apply this same split to every screen below after its features are working, not just Products.

**Login**

- [x] Build login form UI (username, password, submit) — plus bonus: show/hide password toggle
- [x] Build inline error state (fake validation trigger) — checked against a small `MOCK_ACCOUNTS` map (client-side only), with shake animation + red borders; the reference's "Welcome back" success animation + real redirect stays deferred to Part 3.2 (needs a real API response, not mock)

**Category Management** _(simplest — single field, minimal states)_

- [x] Build category list with mock data
- [x] Build add/edit modal
- [x] Build blocked-delete state (mock condition: category has products)
- [x] Build category detail drawer (click a category card → side panel listing its assigned products, with rename/delete actions) — added 2026-07 per `docs/references/category_management.html`, not in the original PRD-derived checklist
- [x] Build empty state (no categories) — missing from the original checklist (found 2026-07 while building Products' empty state); `category_management.html` already defines it (`if (total === 0)`), just never implemented
- [x] Refactor into `lib/types.ts` + `lib/validators/category.ts` + `lib/mock-categories.ts` + `components/categories/*.tsx` split (avoid god file) — see "Refactor convention" note above; pattern already applied to Products as the reference

**Product Management** _(more fields + search/filter than Category)_

- [x] Build product list (table/grid) with mock paginated data
- [x] Build search + category filter controls
- [x] Build add/edit product form (modal), with `zod` validation wired even though submit is fake
- [x] Build delete confirmation dialog
- [x] Build empty state (no products) — plus a separate "no results" state when search/filter matches nothing (different icon/message, no CTA)
- [x] Build product detail drawer (click a row → side panel: image/avatar, category, price, stock, barcode, sold-in-transactions count, edit/delete actions for admin) — added 2026-07 per `docs/references/product_management.html`, not in the original PRD-derived checklist
- [x] Build "Reload" button that triggers the loading-skeleton state (fake/manual for now) — added 2026-07 per `docs/references/product_management.html`. **Not building:** the reference's "View as Admin/Cashier" toggle switch — that's a demo-only affordance for previewing both roles in the static mockup; our real app already derives role from `currentUser` (mocked in `(staff)/layout.tsx` since Part 1.3), so admin/cashier visibility should read from that, never a manual switch
- [x] Refactor into `lib/types.ts` + `lib/validators/product.ts` + `lib/mock-products.ts` + `components/products/*.tsx` split (avoid god file) — `page.tsx` reduced from ~920 to ~210 lines; this is the reference pattern for the "Refactor convention" noted above

**User Management** _(similar to Product, plus 2 extra confirmation flows)_

- [x] Build user list table with role/status badges, mock data
- [x] Build search + role filter controls — missing from the original checklist (found 2026-07, same gap pattern as Product/Category's search+filter); added for consistency with the other two list screens
- [x] Build add/edit user form (name, username, temporary password + "Generate password" button in add mode only, role toggle) — **no active/inactive toggle in the form** (decided 2026-07); activation state is only changed via the separate Deactivate/Reactivate dialog, not editable inline in the form — uses `zod` validation (decided 2026-07, for consistency with Products) via a mode-aware schema factory (`lib/validators/user.ts`)
- [x] Build reset password confirmation dialog — full 2-stage flow (decided 2026-07): confirm → generate + display the new temporary password with a "Copied" indicator, not a single-step confirmation
- [x] Build deactivate/reactivate confirmation dialog (bidirectional — same dialog, content flips based on current `active` state)
- [x] Build user detail drawer (click a row → side panel: avatar/initials, role + status pills, username, role description, joined date, last active; Edit + Reset password actions) — added 2026-07 per `docs/references/user_management.html`, not in the original PRD-derived checklist
- [x] Row-level quick actions: 2 icon buttons (Edit, Deactivate) matching Product/Category's inline-icon-button pattern — **not** the reference's ⋮ dropdown menu (decided 2026-07 for consistency across list screens); Reset password is only reachable via the detail drawer, not a row-level action
- [x] Built directly as a split structure from the start (`lib/types.ts`, `lib/validators/user.ts`, `lib/mock-users.ts`, `components/users/*.tsx`, thin `page.tsx`) — no separate refactor pass needed, unlike Product/Category which were refactored after the fact

**Stock Adjustment** _(small form + history list, naturally follows Product)_

- [x] Build as a standalone dedicated page (`/stock`, own sidebar nav item) — corrected 2026-07: originally worded as "accessible from product detail," but `docs/references/stock_management.html` is a full master-detail page layout, not a modal/tab off the product screen
- [x] Build product list/picker panel (search + All/Low-stock/Out-of-stock filter, clickable rows with avatar, category, stock count, color-coded status dot) — left panel of the master-detail layout, laid out flush against the page header/sidebar per the reference (2026-07)
- [x] Build selected-product header card in the right panel (avatar, name, category, current stock)
- [x] Build adjustment form (Increase/Decrease toggle, quantity input, preset reason chips + free-text reason, live "current → new stock" preview, submit button)
- [x] Build confirm-adjustment dialog (icon/color/title/message vary by increase vs decrease, shows current → new stock level preview)
- [x] Build adjustment history list, mock data, color-coded increase/decrease entries
- [x] Build success toast notification after an adjustment is recorded
- [x] Refactor into `lib/types.ts` (already has `StockAdjustment`) + `components/stock/*.tsx` split (avoid god file) — see "Refactor convention" note above; `page.tsx` now a thin orchestrator wiring `ProductPickerList`, `ProductHeaderCard`, `AdjustmentForm`, `AdjustmentHistory`, `ConfirmAdjustmentDialog`

**Transaction History** _(read/filter-heavy, plus a void flow)_

- [x] Build summary stat cards (Gross sales, Transactions, Avg. ticket, Voided count) — added 2026-07 per `docs/references/transaction_history.html`, not in the original PRD-derived checklist
- [x] Build transaction list with mock data: search by receipt no., status filter (All/Completed/Voided), cashier filter (admin-only visibility), date range shown as a **static label for now** — decided 2026-07: the reference itself never wires the date picker to real filtering (it's a fixed "Jul 7 – Jul 8, 2026" display), so a working calendar/date-range picker is deferred until it's actually needed, not built here
- [x] Build empty state (no transactions match filters) — same omission-fix pattern as Category/Product
- [x] Build pagination footer — same pattern as Products
- [x] Build transaction detail drawer with mock line items (receipt id, time, cashier, items with qty/price, subtotal/payment/change/total)
- [x] Build void action + required-reason confirmation dialog — preset reason chips + free-text textarea, same pattern as Stock Adjustment's reason field
- [x] Build voided-state visual treatment: banner (voided-by + reason) on the detail drawer, strikethrough total + voided status pill on the list row
- [x] Refactor stat cards + filter row + table + pagination out of `page.tsx` into `components/transactions/*.tsx` (avoid god file) — see "Refactor convention" note above; split into `TransactionStats`, `TransactionFilters`, `TransactionsTable`, alongside the already-separate `TransactionDetailSheet`/`VoidTransactionDialog`
- **Not building:** the reference's "View as Admin/Cashier" toggle — same reasoning as Product Management (2026-07): role comes from `currentUser` (mocked in `(staff)/layout.tsx`), never a manual switch

**POS / Checkout** _(most interaction states — real Zustand cart, live calculation, multiple error/success states)_

- [x] Build product grid using the existing `lib/mock-products.ts` (shared with Products/Stock, not a separate POS-only list) — decided 2026-07: keeps stock/price consistent across every screen instead of duplicating mock data, even though `docs/references/checkout.html`'s own demo data has more products/categories
- [x] Build top info bar: live date + ticking clock (updates every second via `setInterval`), shift label — added 2026-07 per `docs/references/checkout.html`, not in the original PRD-derived checklist; also restructured the left column into a fixed top zone (title/date-time/search/sort/chips) + separately-scrolling grid below, matching the reference's layout instead of scrolling everything together. **Hydration lesson (2026-07):** the clock's `now` state must start as `null` (same on server and client) and only get a real `new Date()` inside `useEffect` — initializing it eagerly via `useState(() => new Date())` causes a server/client text mismatch (Next.js hydration error), a different problem from the `Date.now()`-in-render **lint** rule we hit earlier on Stock/Transactions
- [x] Build search bar (client-side filter over mock array) — filters by product name only for now; barcode-scan matching can be added once barcode input is wired
- [x] Build sort dropdown (Popular / Name A–Z / Price low→high / Price high→low) — added 2026-07, not in the original checklist; "Popular" sorts by `product.txnCount` (real mock field, already used on the Product detail drawer), not a fake/decorative order like the reference's own demo data
- [x] Build category filter chips (icon + label + product count per category, scrollable strip) — original checklist just said "tabs"; the reference is richer than a plain tab row; categories derived from `products` (same pattern as Products page filter), not from `lib/mock-categories.ts`, so an empty category never shows as a selectable-but-empty option
- [x] Build product tile states: in-cart quantity badge, low-stock badge ("X left"), out-of-stock disabled tile, hover/selected border
- [x] Build empty state (no products match search/category) — built alongside search, now also covers the category filter via the same `filteredProducts` array
- [x] Build cart panel (Zustand store — this is real, not mocked, since it's pure client state) — `stores/cart-store.ts`, tracks `quantities`/`order` by product id (not a copy of product data)
- [x] Build cart empty state ("No items yet") — reuses the shared `EmptyState` component
- [x] Build quantity stepper, remove item, clear cart actions — removing is implicit (decrement to 0), matching the reference (no separate per-line delete button)
- [x] Build insufficient-stock error state — corrected 2026-07: this is **derived automatically** from `cart qty > product.stock` (highlighted cart line + banner + blocked checkout button), not a manually-triggered toggle as originally worded
- [x] Build checkout modal: amount received input, quick-cash suggestion buttons (Exact / round up to 5/10/20), live change calculation (pure frontend math for now), "Complete sale" disabled until received ≥ total — also switched `products` from a direct `MOCK_PRODUCTS` read to local `useState` here, since completing a sale needs to actually decrement stock
- [x] Build on-screen receipt success state: line items, subtotal/paid/change, Print button (decorative, no real print logic), New sale button (resets cart + closes receipt) — receipt ref counter (`TX-2042`, `TX-2043`, ...) is a plain `useState` counter, not `Date.now()`/`Math.random()`, matching the impure-function lint rule we hit earlier
- [x] Refactor `page.tsx` (was 704 lines) into `components/checkout/*.tsx` (avoid god file) — see "Refactor convention" note above; missed when this section's checklist was first expanded, unlike Products/Stock/Transaction History which each got this item from the start. Split into `CheckoutHeader`, `ProductGrid`, `CartPanel`, `CheckoutDialog`, `ReceiptOverlay`; `Receipt` type moved to `lib/types.ts` alongside the other shared types; `page.tsx` is now state + handlers + wiring only

**Sales Dashboard** _(hardest — new charting library + date-range aggregation logic)_

**Data approach (2026-07):** everything on this page except Low Stock uses a small set of hardcoded mock "snapshots," one per date-range period (Today / 7 days / 30 days) — including the delta badges — matching `docs/references/dashboard.html`'s own approach (`lib/mock-dashboard.ts`, one object per period). `MOCK_TRANSACTIONS` only spans 2 days, so computing a real "last 30 days" trend from it would be dishonest, not just simplified. Low Stock is the one exception: it reads real numbers from `lib/mock-products.ts` (same `getStockStatus` already used on Stock/Products), so it never disagrees with what those pages show.

- [x] Install `recharts`
- [x] Build date range selector: 3-option segmented toggle (Today / 7 days / 30 days) + read-only resolved date-range chip — corrected 2026-07: not a full calendar/date-range picker, matches the reference's own simpler control (same pattern as the Transaction History date filter)
- [x] Build 4 headline stat cards (Total sales, Transactions, Avg. ticket, Items sold) with mock numbers, `tabular-nums`, each with a delta badge (↑/↓ % vs previous period + a "vs yesterday" / "vs last week" / "vs prev. 30 days" note) — corrected 2026-07: original checklist only named 2 of the 4 cards and missed the delta badges entirely
- [x] Build main "Sales by hour" (Today) / "Sales by day" (7/30 days) bar chart using `recharts`'s `BarChart` — decided 2026-07: the one real chart on the page (see data-approach note above); bar labels/granularity change per selected period; per-bar color (`<Cell>`) highlights the peak bar in navy, rest in light blue, matching the reference
- [x] Build "Best sellers" list (rank, name, qty sold, horizontal progress bar) — added 2026-07 per reference; plain styled bars, **not** a `recharts` component — it's a ranked list, not a chart; progress bar reuses the `--accent` token (already the same blue as the reference)
- [x] Build "Sales by category" horizontal bar breakdown (% share per category) — added 2026-07 per reference, not in the original checklist; also plain styled bars, not `recharts`
- [x] Build "Low stock" panel (product name + status badge, "Out of stock" vs "X left") — added 2026-07 per reference; the one section on this page backed by real data (`lib/mock-products.ts`), not a per-period snapshot; sorted ascending by stock, doesn't change with the period toggle (current stock isn't a "per period" concept)
- [x] Build empty state (zeroed totals) — no reference guidance for this (the reference's own mock data is never empty); follows the same `EmptyState` pattern used on every other list screen. Unreachable via the UI right now since all 3 mock snapshots in `lib/mock-dashboard.ts` have real (non-zero) numbers — verified by temporarily hardcoding `hasNoSales = true`, not by triggering it through the toggle; will become reachable for real once Part 3 wires in a live API that can return a genuinely empty day
- [x] Refactor `page.tsx` (was 335 lines) into `components/dashboard/*.tsx` (avoid god file) — see "Refactor convention" note above; add this item up front next time a section's checklist gets expanded (missed on both this section and Checkout until pointed out afterward). Split into `DashboardHeader`, `StatCard` + `StatsGrid`, `SalesChart`, `BestSellers`, `CategoryBreakdown`, `LowStock`; `page.tsx` is now state + the empty-state branch + wiring only

### 1.5 Frontend State Wiring (still mock-backed)

**Scope decision (2026-07):** the TanStack Query provider + hooks are set up now as a **pattern**, demonstrated on 2 modules only (Products, Transactions) — existing pages are **not** rewired to consume them yet; every page keeps its current `useState(MOCK_X)` for now. Rewiring each remaining screen (Categories, Users, Stock, Dashboard) to the hook layer is deferred to happen screen-by-screen alongside Part 3.3, where the mock hook body gets swapped for a real API call anyway — folding both changes into one pass avoids touching each page twice.

- [x] Set up `stores/cart-store.ts` (Zustand) — this is real, cart logic doesn't need a backend — already built during POS/Checkout (`quantities`/`order` state + `addItem`/`increment`/`decrement`/`removeItem`/`clearCart` actions)
- [x] Install `@tanstack/react-query`
- [x] Set up `lib/query-client.ts` — a shared `QueryClient` instance (sane defaults: e.g. `staleTime` so mock data doesn't needlessly "refetch" on every focus)
- [x] Wrap the app in a `QueryClientProvider` — needs its own small `"use client"` provider component (`components/providers/query-provider.tsx`), since `app/layout.tsx` is a Server Component and can't hold client-side context directly; `RootLayout` itself stays a Server Component, it just renders the client provider around `children`
- [x] Write `hooks/use-products.ts` as the reference pattern: `useQuery({ queryKey: ["products"], queryFn: ... })` wrapping `MOCK_PRODUCTS` in an artificial `setTimeout` delay, so the loading state is genuine rather than instant — **not yet wired into `app/(staff)/products/page.tsx`**, just the hook itself
- [x] Write `hooks/use-transactions.ts` following the same pattern, as the second example — also not wired into the page yet
- [ ] Leave `use-categories.ts`, `use-users.ts`, `use-stock-adjustments.ts`, `use-dashboard.ts` for later — build each one only when its page actually gets rewired during Part 3.3, not now

**Checkpoint:** at the end of Part 1, you should be able to click through the entire app — login (fake), build a cart, "checkout" (fake), browse products, manage categories/users, view fake transaction history and dashboard — with zero database or API routes existing yet. This is your clickable template.

---

## Part 2 — Backend (Route Handlers + Drizzle, Same App)

Goal of this part: fill in `app/api/v1/**`, `lib/db/`, and `lib/services/` inside the **same Next.js app** — a fully working API you can test independently with `curl`/Postman before touching the frontend again, following `TECH_SPEC.md`.

### 2.1 Database Setup

- [x] Set up PostgreSQL locally via Docker Compose — `docker-compose.yml` (service `db`, Postgres 16, named volume for persistence) + `.env` with `DATABASE_URL`; container running as `brewpoint-db-1`
- [x] Install `drizzle-orm`, `drizzle-kit`, `postgres` (postgres.js driver)
- [x] Write `lib/db/schema.ts` with all 6 tables from `TECH_SPEC.md` Section 3.3
- [x] Write `lib/db/index.ts` — Drizzle client instance
- [x] Configure `drizzle.config.ts`
- [x] Run `CREATE EXTENSION pgcrypto` (or use `defaultRandom()`, which Drizzle handles without the extension via `gen_random_uuid()` — confirm which your Postgres version needs) — confirmed 2026-07: not needed, `gen_random_uuid()` has been built into Postgres core since v13, and `docker-compose.yml` runs `postgres:16`
- [x] Run `npx drizzle-kit generate` → review the generated SQL → `npx drizzle-kit migrate` — verified directly against the container (`docker exec brewpoint-db-1 psql ...`): all 6 tables exist in `brewpoint_db`
- [x] Add the manual index migration from `TECH_SPEC.md` Section 3.4 (GIN/full-text index isn't native to Drizzle's schema syntax yet) — `drizzle/0001_add-indexes.sql` via `drizzle-kit generate --custom`; verified all 9 indexes exist (`docker exec brewpoint-db-1 psql ... -c "\di"`)
- [x] Write `lib/db/seed.ts` — one admin user, a couple of categories, a handful of products. Run it. — `npm run db:seed` (`tsx --env-file=.env lib/db/seed.ts`); the `--env-file` flag was needed since, unlike Next.js, `tsx` doesn't auto-load `.env` — without it, `postgres.js` silently fell back to a default connection using the OS username instead of erroring loudly. Verified via `docker exec brewpoint-db-1 psql ...`: 1 admin (`Marcus Bell`), 2 categories, 3 products

### 2.2 Helpers / Shared Packages (build these before the feature modules — everything depends on them)

- [x] `lib/api-response.ts` — standard success/error envelope helpers (`ok()`, `fail()`)
- [x] `lib/app-error.ts` — `AppError` class + error code → HTTP status mapping (per `TECH_SPEC.md` Section 8)
- [x] A shared wrapper/handler utility that catches `AppError` thrown inside a Route Handler and formats the envelope + status automatically — `lib/api-handler.ts`, `withErrorHandling()`
- [x] `lib/auth/jwt.ts` — sign/verify JWT using `jose`
- [x] Password hashing helper using `bcryptjs` — built early, ahead of this checklist's order, as `lib/auth/password.ts` (`hashPassword`/`verifyPassword`) — `lib/db/seed.ts` in 2.1 needed it immediately (`password_hash` is `NOT NULL`), so it couldn't wait until this section
- [x] `lib/auth/session.ts` — `requireAuth(role?)` helper (per `TECH_SPEC.md` Section 7) — dropped the unused `req` param from the spec's example signature, and corrected `cookies()`/`verifyJwt()` to be properly `await`-ed (both are async in our Next.js/jose versions, unlike the spec's illustrative snippet)
- [x] `middleware.ts` — redirect-level protection for unauthenticated requests — must live at the project root (sibling to `package.json`), not under `lib/`; caught and moved from `lib/auth/middleware.ts` after it was first created in the wrong place
- [x] `lib/validators/` — start the shared `zod` schema files (one per module), reused later by both Route Handlers and frontend forms — `product.ts`/`user.ts` already exist from Part 1's frontend forms; decided 2026-07 to defer creating the rest (categories, stock-adjustments, etc.) until each module's Route Handler section (2.4–2.8) actually needs them, same pattern as the 1.5 TanStack Query hooks decision. **Known gap to fix in 2.6:** `productSchema.category` currently stores the category **name** (fine for Part 1's mock data), but `lib/db/schema.ts` needs `categoryId` (a UUID) — this schema must be adjusted when the real Products Route Handler is built, not before

### 2.3 Auth Routes

- [x] `app/api/v1/auth/login/route.ts` — verify credentials, issue JWT in httpOnly cookie — same error message ("Invalid username or password") for both "user not found" and "wrong password", so a caller can't enumerate valid usernames; not yet tested with `curl` (see last item below, waiting on logout/me too)
- [x] `app/api/v1/auth/logout/route.ts` — clear cookie
- [x] `app/api/v1/auth/me/route.ts` — return current user from JWT claims — added `name` to `SessionClaims`/`signJwt` payload (in `login/route.ts`) so `/me` can answer straight from the decoded token, no extra DB lookup, matching this item's own wording ("from JWT claims")
- [x] Test all three with `curl`/Postman before moving on — login (correct + wrong password), `/me` with and without cookie (blocked by `middleware.ts` when missing), logout then `/me` again (correctly 401 after cookie cleared)

### 2.4 User Module (Admin only)

- [x] `lib/services/user-service.ts` — create, list, get by ID, update, reset password, deactivate; unique username check, "can't deactivate last active admin" rule
- [x] `app/api/v1/users/route.ts` (GET list / POST create), `app/api/v1/users/[id]/route.ts` (PUT update / PATCH `isActive` toggle), `.../reset-password/route.ts` (POST) — wired with `requireAuth("admin")` in every handler. Added backend-specific `createUserBodySchema`/`updateUserBodySchema`/`resetPasswordBodySchema`/`setActiveBodySchema` to `lib/validators/user.ts` (lowercase `role` to match `roleEnum`, no `existingUsernames` param — the frontend-only `getUserFormSchema` wasn't reusable as-is). Also fixed `lib/api-handler.ts` to catch `ZodError` → `400 BAD_REQUEST` (it was only catching `AppError` before, so a failed `.parse()` would've fallen through to a generic 500) — this fix benefits every future route handler, not just this module
- [x] Test every endpoint independently — list, create, duplicate-username (409), update, reset-password, deactivate, deactivate-last-admin (409), and no-session access (401), all via `curl`

### 2.5 Category Module

- [x] `lib/services/category-service.ts` — `listCategories`, `getCategoryById`, `createCategory` (unique name check, same proactive-`SELECT`-before-write pattern as `user-service.ts`), `updateCategory` (unique name check excluding self), `deleteCategory` (block with `AppError("CONFLICT", ..., 409)` if any product still references this category — the FK has no `ON DELETE CASCADE`, so an unchecked delete would otherwise surface as a raw Postgres constraint error instead of a clean envelope)
- [x] `lib/validators/category.ts` (new file — Category Management's frontend form used manual `useState` validation, not `zod`, so there's nothing to reuse here) — `createCategoryBodySchema`/`updateCategoryBodySchema`, just a required `name` string
- [x] `app/api/v1/categories/route.ts` — GET (list, any authenticated user, no role restriction — `requireAuth()` with no role arg) / POST (create, `requireAuth("admin")`)
- [x] `app/api/v1/categories/[id]/route.ts` — PUT (update, admin only) / DELETE (delete, admin only)
- [x] Test every endpoint via `curl`: list, create, duplicate-name (409), update, delete blocked by existing products (409), delete succeeds once unblocked, cashier attempting a write (403)

### 2.6 Product Module

- [x] `lib/services/product-service.ts` — `searchProducts({ search?, categoryId?, page, pageSize })` returning `{ items, total }` (name/barcode match — reuse the `idx_products_name` GIN index from 2.1 §3.4 via `to_tsvector`/`plainto_tsquery`, not a plain `ILIKE` scan — plus category filter and pagination). **Known limitation, accepted 2026-08:** `plainto_tsquery` only matches whole words or word-start prefixes — a mid/end fragment like "ffee" for "Coffee" won't match anything; not fixed with `pg_trgm` since cashiers mostly type the start of a name or tap the grid directly, and TECH_SPEC.md's own plan never called for trigram search, `getProductById`, `createProduct` (barcode uniqueness check only when a barcode is provided — it's optional/nullable in the schema; validates `categoryId` actually exists by calling `getCategoryById` from `category-service.ts`, turning a bad id into a clean `NOT_FOUND` instead of a raw FK-violation error), `updateProduct` (same 2 checks, barcode check excludes self), `deleteProduct` (**soft vs. hard delete** — check `transaction_items` for any row referencing this product first: if none exist, hard-delete the row for real; if it's been sold at least once, soft-delete via `is_active = false` instead, to keep historical `transaction_items` pointing at a valid row. This matches what the frontend's `delete-product-dialog.tsx` already promised — soft-delete isn't the *only* path like the original wording of this item implied)
- [x] `lib/validators/product.ts` — add backend-specific `createProductBodySchema`/`updateProductBodySchema` alongside the existing frontend-only `productSchema` (same reasoning as 2.4/2.5: the frontend schema's `category` field stores the category **name**, not `categoryId`, so it isn't directly reusable as the API body shape). `price` stays a decimal **string** in the request body — Postgres `numeric` columns are strings on the Drizzle/JS side too, per `CLAUDE.md`'s money rule, never a JS float — while `stockQuantity` is a real integer this time, not a string like the frontend form's HTML-input-driven `stock` field
- [x] `app/api/v1/products/route.ts` — GET (list/search, any authenticated user — `search`/`categoryId`/`page` as query params) / POST (create, admin only)
- [x] `app/api/v1/products/[id]/route.ts` — PUT (update, admin only) / DELETE (soft-or-hard per the rule above, admin only)
- [x] Test every endpoint via `curl`: list, search by name, search by barcode, filter by category, pagination, create, duplicate-barcode (409), nonexistent `categoryId` (404, not a raw FK error), update, hard-delete a never-sold product (confirm the row is actually gone), soft-delete a product with transaction history (confirm the row survives with `is_active: false`), cashier attempting a write (403)

### 2.7 Transaction Module (the critical one)

**New pattern here, different from 2.4–2.6:** every service so far (`user`/`category`/`product`) avoided race conditions with a proactive `SELECT`-before-write check. That doesn't work for stock — two cashiers can pass the "is there enough stock" check at the exact same instant, on the same product, before either one writes. `checkout()` instead uses **row locking** (`SELECT ... FOR UPDATE`, via Drizzle's `.for("update")`) inside `db.transaction()`: the first checkout to reach a given product row locks it, the second one *waits* until the first commits or rolls back, then re-reads the now-updated stock. This is the one place in the whole backend where "check first, then write" isn't safe enough on its own.

- [x] `lib/validators/transaction.ts` (new file) — `checkoutBodySchema` (`items: { productId: uuid, quantity: positive int }[]`, non-empty array; `amountReceived` as a decimal string, same as `price` elsewhere), `voidBodySchema` (`reason`, required non-empty string — matches the frontend's `VoidTransactionDialog`, which already treats a reason as mandatory). Used `z.uuid()` (Zod v4 top-level format validator), not the deprecated `z.string().uuid()` — also fixed the same deprecated form in `product.ts`'s `categoryId` while at it
- [x] `lib/services/transaction-service.ts` — `checkout(cashierId, items, amountReceived)` exactly as in `TECH_SPEC.md` Section 5: wrapped in `db.transaction()`, locks each product row with `.for("update")` before checking stock, throws `INSUFFICIENT_STOCK` (409) or `NOT_FOUND` mid-transaction to trigger an automatic rollback (Drizzle rolls back on any thrown error, `AppError` included), snapshots `productNameSnapshot`/`unitPriceSnapshot` onto each `transaction_items` row per the snapshotting rule in `CLAUDE.md`. `cashierId` comes from `requireAuth()`'s decoded session, **never** from the request body — the client must not be able to claim a transaction happened under a different cashier's name. `lineItems` typed as `Omit<typeof transactionItems.$inferInsert, "transactionId">[]` — that field only exists once the `transactions` row is inserted after the loop, so the full `$inferInsert` shape doesn't fit yet at push-time
- [x] Same service — `listTransactions({ cashierId?, status?, page })`: if the caller is a cashier, force `cashierId` to their own id server-side (ignore any `cashierId` they try to pass) so they can only ever see their own sales; only an admin caller may filter by an arbitrary `cashierId` or see everyone's — matches the frontend's "cashier filter (admin-only visibility)" from Part 1.4
- [x] Same service — `getTransactionById(id)`
- [x] Same service — `voidTransaction(id, voidedBy, reason)`: reject if already `voided` (can't void twice — `AppError("CONFLICT", ..., 409)`), wrapped in its own `db.transaction()` since it's a multi-step write (mark the transaction voided + restore stock for every line item) that must all succeed or all roll back together; sets `status: "voided"`, `voidedBy`, `voidedReason`, `voidedAt`, and increments `stockQuantity` back up on each referenced product
- [x] `app/api/v1/transactions/route.ts` — GET (list, any authenticated user, scoped per the rule above) / POST (checkout, any authenticated user — cashiers check out sales same as admins)
- [x] `app/api/v1/transactions/[id]/route.ts` — GET (detail; a cashier should only be able to fetch their own — same scoping as the list, but re-checked explicitly here since this route looks a transaction up directly by id rather than through the filtered list query)
- [x] `app/api/v1/transactions/[id]/void/route.ts` — POST (void, admin only — matches the frontend, which only ever shows the void action to admins). **Caught and fixed (2026-08):** first created at `app/api/v1/transactions/void/route.ts` — missing the `[id]` folder segment entirely, so it had no way to receive which transaction to void; moved into the correct nested path
- [ ] **Manual concurrency test:** fire two simultaneous checkout `curl` requests for the same low-stock product (e.g. stock = 1, both requests buy 1) and confirm exactly one succeeds with `200` and the other gets `INSUFFICIENT_STOCK` (409) — this is what actually proves the row lock works, reading the code isn't enough, don't skip this. **Kept deliberately (2026-08)** even though the general CRUD `curl` pass below was dropped — this is the one test that can't be replaced by reading the code or a code review
- ~~Test the rest via `curl`: successful checkout, void, error cases~~ — dropped 2026-08, covered by code review instead of a full manual `curl` pass (unlike every other module so far)

### 2.8 Stock Adjustment Module

**Scope note (2026-08):** the whole feature is admin-only — not just the write side, like the pattern used for categories/products (read = anyone logged in, write = admin). The Stock nav item itself is `roles: ["admin"]` in `sidebar-nav.tsx`, so cashiers never see this screen at all; matching that, both `GET` and `POST` below require `requireAuth("admin")`.

- [x] `lib/validators/stock-adjustment.ts` (new file) — `createAdjustmentBodySchema`: `adjustmentType: z.enum(["increase", "decrease"])`, `quantity: z.number().int().positive()`, `reason` (required non-empty string — matches the frontend's preset-reason-chips + textarea, which already treats a reason as mandatory)
- [x] `lib/services/stock-adjustment-service.ts` — `createStockAdjustment(productId, adminId, input)`: wrapped in `db.transaction()`, locks the product row with `.for("update")` (same reasoning as `checkout()` — two admins adjusting the same product's stock at once is the same class of race condition), computes the new stock level, **rejects** (doesn't silently clamp) a `decrease` that would take stock below 0 — matches the frontend's own validation (`"Can't remove more than current stock"`), not the reference mockup's `Math.max(0, ...)` clamp which just hides the mismatch. Inserts one row into `stock_adjustments` (append-only per `CLAUDE.md` — insert + read only, this service never updates or deletes an adjustment row) and updates `products.stockQuantity` in the same transaction
- [x] Same service — `listStockAdjustments(productId)`: full history for one product, ordered newest-first, no pagination (per-product history is naturally small — unlike `searchProducts`/`listTransactions`, which are unbounded across the whole catalog/all sales)
- [x] `app/api/v1/products/[id]/stock-adjustments/route.ts` — GET (history for this product) / POST (create adjustment) — both admin only, see scope note above
- ~~Test via `curl`: increase, decrease within available stock, decrease past available stock, view history, cashier attempting either verb (403), nonexistent product (404)~~ — skipped 2026-08, same call as the 2.7 general CRUD pass (covered by code review instead)

### 2.9 Dashboard Module

**Scope note (2026-08):** `PRD.md` §8 marks this a **Should**-priority, deliberately "basic" feature — total sales + transaction count for a date range, and a simple best-sellers list ranked by quantity. The frontend's `lib/mock-dashboard.ts` (built in Part 1) is much richer than that (period-over-period deltas, hourly/daily bar-chart series, category breakdown percentages) — that's mock-only decoration for now, not a PRD requirement. `TECH_SPEC.md`'s own file layout only lists two dashboard routes (`summary`, `best-sellers`), confirming the smaller scope. Don't build endpoints for deltas/bars/category-breakdown here; if Part 3 integration needs them, that's a separate decision to make then, not now. Admin only, per PRD's "As an Admin, I want to see..." — no cashier user story exists for this feature. The `LowStock` component on the dashboard page doesn't need a new endpoint either — it already reads from `MOCK_PRODUCTS` client-side and will switch to the existing `GET /api/v1/products` list in Part 3.

- [x] `lib/services/dashboard-service.ts` — `getSalesSummary({ from, to })`: total `totalAmount` and count of `transactions` in range, filtered to `status = "completed"` (excludes voided, per PRD acceptance criteria) and `createdAt` between `from`/`to`. New pattern here: this is the first place summing a column across rows is needed — use Drizzle's `sum()` aggregate helper (not `count()`, which you've used before but only counts rows, not sums a value)
- [x] Same service — `getBestSellers({ from, to, limit })`: ranks products by total quantity sold in range. New pattern: needs a **join** (`transactionItems` → `transactions`, to filter by the parent transaction's date/status — `transaction_items` itself has no `createdAt` or `status` column) plus **`GROUP BY` + `SUM`** (group by `productId` + `productNameSnapshot`, sum `quantity`, `orderBy(desc(sum))`, `limit`). Use `transactionItems.productNameSnapshot` for display names — deliberately avoids a second join to `products`, and it's the historically-correct name anyway (a renamed/deleted product shouldn't rewrite past best-seller stats, same snapshotting principle as the receipts themselves)
- [x] Both — if `from`/`to` aren't supplied, default to "today" (start of day → now), per PRD's "date range defaults to today on load"
- [x] `app/api/v1/dashboard/summary/route.ts` — GET, admin only, reads `from`/`to` query params
- [x] `app/api/v1/dashboard/best-sellers/route.ts` — GET, admin only, reads `from`/`to`/`limit` query params
- [x] Tested via `curl` (2026-08): baseline `today` returned zeroed/empty; checkout (2x Cappuccino, 8.50) made both `summary` (`totalSales: "8.50"`, `transactionCount: 1`) and `best-sellers` (Cappuccino, qty 2) reflect it; voiding that same transaction dropped both back to zero/empty, proving the `status = "completed"` filter actually excludes voided rows rather than just running without error; wide date range (`2020-01-01`–`2026-12-31`) and `best-sellers?limit=1` both parsed correctly; cashier attempting either route got `403 FORBIDDEN`
- [x] **Bug found and fixed (2026-08):** `requireAuth()` in `lib/auth/session.ts` threw `AppError("UNAUTHORIZED", ...)` and `AppError("FORBIDDEN", ...)` without an explicit `status` argument, so both silently fell back to `AppError`'s default `400` — every route gated by `requireAuth()` across the whole backend (not just dashboard) was returning `400` instead of `401`/`403`. Caught via the cashier-403 test above (`403` expected, `400` returned). Fixed by adding `401`/`403` explicitly on those two throws; re-tested and confirmed correct status codes now return

### 2.10 Full API Pass

**Tooling note (2026-08):** testing is done in Postman, not raw terminal `curl` — Postman manages the session cookie automatically per domain once you're logged in within the collection, so no manual cookie-jar juggling needed.

- [x] `docs/api-curl-reference.md` (new file) — one `curl` command per endpoint across every module (Auth, Users, Categories, Products, Transactions, Stock Adjustments, Dashboard), happy-path plus the key error cases from `TECH_SPEC.md` Section 8 (wrong password, wrong role, not found, insufficient stock, duplicate, etc.), each with its expected status/error code noted. Paste each into Postman ("Import" → raw text, or paste into a new request's address bar) to build the collection
- [x] Ran every request in the built Postman collection (2026-08-23) — actual results matched what's noted in the reference file

**Checkpoint:** at the end of Part 2, the entire API works correctly on its own — provable via Postman/curl — even though the frontend built in Part 1 still shows mock data. The two halves haven't been connected yet.

---

## Part 3 — Integration (Connect Frontend to Backend)

Goal: replace every mock in Part 1 with real calls to the Route Handlers built in Part 2. Because it's all one app on the same origin, there's no CORS configuration and the httpOnly cookie is sent automatically — integration here is simpler than a two-service setup.

### 3.1 API Client Setup

**Scope note (2026-08):** the current `lib/types.ts` (Part 1 mock era) doesn't just need new entries — it diverges from the real API in ways that matter, not just naming: numeric `id` (real ids are UUID strings), `price: number` / `received: number` (real `NUMERIC` columns are strings — `CLAUDE.md`'s money rule is "never JS floats", so a translation layer that turns the real string back into a number would directly violate that), capitalized `role: "Admin" | "Cashier"` (real enum is lowercase), `category: string` holding a category *name* (real product rows hold `categoryId`, a UUID, not a name). Given the money rule specifically rules out a shape-preserving adapter, the right move is to **rebuild `lib/types.ts` to mirror the real API**, not bend the real data back into the old mock shape. That means several Part 1 components will need small adjustments once their module is wired in 3.3 (e.g. anywhere doing `.price.toFixed(2)` on what's now a string, or checking `role === "Admin"`) — not now, but flagging it here so it isn't a surprise later.

- [x] `lib/api-error.ts` (new file) — `ApiError extends Error` (`code`, `message`, `status`), mirroring `lib/app-error.ts`'s own separate-file convention on the backend, rather than folding it into `api-client.ts`
- [x] `lib/api-client.ts` — `apiFetch<T>(path, options?)`: prepends `/api/v1` to `path`, sets `Content-Type: application/json` only when a body is present, calls `fetch`. No `credentials: "include"` needed — frontend and API share the same origin, and fetch's default credentials mode (`"same-origin"`) already sends the `session` cookie automatically
- [x] Same file — unwraps the `{ success, data }` / `{ success: false, error: { code, message } }` envelope from `lib/api-response.ts`: on `success: true`, returns `data` (typed `T`); on `success: false`, throws `ApiError` (carrying the HTTP status too) so callers can distinguish e.g. `error.code === "INSUFFICIENT_STOCK"` in the checkout flow (3.3)
- [x] Same file — thin convenience wrappers on top (`apiGet`, `apiPost`, `apiPut`, `apiPatch`, `apiDelete`), same "one core mechanism, thin named wrappers" shape as `lib/api-handler.ts` on the backend
- [x] `lib/types.ts` — rebuilt using Drizzle's `$inferSelect` off `lib/db/schema.ts` (`User`, `Category`, `Product`, `Transaction`, `TransactionItem`, `StockAdjustment`) plus custom composite types for non-raw-row shapes (`ProductSearchResult`, `TransactionDetail` via `Transaction & { items: TransactionItem[] }`, `BestSeller`, `DashboardSummary`). Kept `ProductStockStatus` (UI-derived, not an API shape). `Receipt` deliberately left undecided — revisit when 3.3 actually builds the POS/Checkout module's receipt UI, not before
- [x] **Known consequence, accepted deliberately (2026-08):** replacing `lib/types.ts` immediately broke `tsc` across dozens of Part 1 files still written against the old mock shapes (`product.stock`, `product.category`, `.price.toFixed(2)` on what's now a string, etc.) — TypeScript checks the whole project, not just the module being worked on, so this couldn't be deferred quietly. **Decision:** accept the red `tsc` and continue through 3.2 in TODO order; each broken file gets fixed as its module is wired in 3.3, not all at once now. Don't be alarmed by `tsc` failures in unrelated-looking files until 3.3 is fully done — this is expected, not a regression to chase down early
- [x] **`passwordHash` leak — found and fixed (2026-08):** `lib/services/user-service.ts` was returning every column (including `passwordHash`) from all 6 user-returning functions via bare `db.select()`/`.returning()`. Fixed with a shared `safeUserColumns` object (every column except `passwordHash`), used via `db.select(safeUserColumns)`/`.returning(safeUserColumns)` in `listUsers`, `getUserById`, `createUser`, `updateUser`, `resetPassword`, `setUserActive` — except the internal `activeAdmins` lookup inside `setUserActive`, left as a bare `db.select()` since its result is only used for `.length` and never returned to the client. `lib/types.ts`'s `User` type updated to `Omit<typeof users.$inferSelect, "passwordHash">` to match reality instead of hiding a real leak. Re-tested via `curl` (create + list) — `passwordHash` confirmed gone from both responses

### 3.2 Real Authentication

**Scope note (2026-08):** checked how Part 1 actually wired "current user" — turns out there's no `useAuth`/context at all yet. `app/(staff)/layout.tsx` (a Server Component, wraps every staff page) directly imports a static `lib/mock-current-user.ts` (`{ name: "Jon Doe", role: "admin" }`) and passes `mockCurrentUser.role` straight into `<SidebarNav role={...} />`, plus renders the name/role in the sidebar footer. The logout button there has no `onClick` at all yet either. So this section is a bigger structural change than "wire a couple of handlers" — it's introducing the app's first real client-side session state.

- [x] **Architecture decision, implemented:** `components/shared/staff-user-panel.tsx` (new Client Component) calls `useCurrentUser()`, renders `null` while loading, redirects to `/login` via `useEffect` if the query errors (handles an expired-but-present session cookie — `middleware.ts` only checks the cookie exists, not that the JWT still verifies), then renders `SidebarNav` + the avatar/name/role footer (moved as-is from `layout.tsx`, just reading `user.name`/`user.role` instead of `mockCurrentUser`). Logout handler: `apiPost("/auth/logout")` → `queryClient.removeQueries({ queryKey: ["current-user"] })` (clear the now-stale cache) → `router.push("/login")`, in that order. `StaffLayout` stays a Server Component; only this smaller piece needed the `"use client"` boundary
- [x] `hooks/use-current-user.ts` (new file) — the first hook actually wired to `lib/api-client.ts` instead of a mock `setTimeout`; this is the reference pattern the rest of 3.3's real hooks will follow. **Corrected while building (2026-08):** `GET /auth/me` returns `SessionClaims` (`{ userId, username, name, role }` from `lib/auth/jwt.ts`), not `User` from `lib/types.ts` — different shape (`userId` not `id`, no `isActive`/`createdAt`) since one's a JWT payload and the other's a DB row. Used `apiGet<SessionClaims>("/auth/me")` with `import type { SessionClaims } from "@/lib/auth/jwt"` (type-only import, so none of that file's server-only code — `jose`, `JWT_SECRET` — ends up bundled client-side). Also set `retry: false`, since a failed `/auth/me` almost always means a real `401` (not logged in), not a transient network blip worth retrying
- [x] Wired the login form (`app/login/page.tsx`) to `POST /api/v1/auth/login` via `apiPost`; deleted `MOCK_ACCOUNTS` and the old local username/password check entirely. **Another shape mismatch caught while building (2026-08):** the login response is `{ id, username, name, role }` — `id`, not `userId` like `SessionClaims`, and not the full `User` either (no `isActive`/`createdAt`). Typed it as `Pick<User, "id" | "username" | "name" | "role">` (a third distinct shape from the same `User` base, alongside `SessionClaims` — worth remembering these three don't interchange)
- [x] Login success/failure transition states, matching `docs/references/login.html`: on success, swaps the form for the reference's checkmark-circle + "Welcome back, {name}" + "Signing you in…" panel, then `router.push` to the role's landing page (`admin` → `/dashboard`, `cashier` → `/checkout`) per `PRD.md` Flow 1, after a `setTimeout` pause — tuned from an initial 900ms up to **1600ms** per user feedback (felt too abrupt at 900ms). On failure, catches the thrown `ApiError` and reuses the existing `error` state/`animate-shake` from Part 1.4. Button also shows `disabled={loading}` + "Signing in…" text to prevent double-submit
- [x] **Added beyond original scope, user-requested (2026-08):** logout now asks for confirmation first — new `components/shared/logout-confirm-dialog.tsx`, styled like `components/stock/confirm-adjustment-dialog.tsx` (same `Dialog`/`DialogContent` pattern, icon + heading + Cancel/destructive-confirm buttons). `staff-user-panel.tsx`'s logout icon-button now opens the dialog instead of logging out directly; the dialog's `onConfirm` runs the actual `handleLogout`
- [x] Wired the logout button's `onClick` in `staff-user-panel.tsx` to `POST /api/v1/auth/logout` via `apiPost`, then `queryClient.removeQueries({ queryKey: ["current-user"] })`, then `router.push("/login")`. `app/(staff)/layout.tsx` updated: `mockCurrentUser`/`SidebarNav`/`getInitials`/`LogOut` imports removed, the old inline footer `div` deleted, replaced with a single `<StaffUserPanel />` — `tsc` clean on both files
- [x] **Bug found and fixed via manual testing (2026-08):** unauthenticated → `/login` redirect worked, but "admin-only pages reject cashier sessions" **did not** — `middleware.ts` only checked that the `session` cookie existed, never decoded it, so a logged-in cashier could open `/dashboard` or `/users` directly by URL (page shell rendered fine; only the underlying API calls would eventually 403 once wired in 3.3 — but the page itself was fully reachable, a real gap caught by the user hard-navigating to `/dashboard` after logging in as cashier). Fixed: added `lib/route-roles.ts` (new file, single `path → allowed roles` map — the one source of truth, replacing the `roles` arrays that were duplicated inline in `sidebar-nav.tsx`'s `NAV_ITEMS`, which now reads from this map instead). `middleware.ts` is now `async`, calls `verifyJwt` (safe on Edge — `jose`, per the original reason it was chosen over `jsonwebtoken`) for any path with an entry in `ROUTE_ROLES`, and redirects to `/checkout` if the session's role isn't allowed (or to `/login` if the token fails to verify at all, e.g. expired). This is a UX-layer improvement, not a replacement for `requireAuth(role)` inside each Route Handler — that's still the real security boundary per `CLAUDE.md`, this just stops a cashier from seeing an admin page's shell at all. Re-tested via `curl`: cashier → `/dashboard` now `307` to `/checkout`; cashier → `/checkout` stays `200`; admin → `/dashboard` and `/users` both stay `200`
- [ ] **`lib/mock-current-user.ts` — NOT deleted yet (checked 2026-08):** still referenced by `products/page.tsx`, `transactions/page.tsx`, `stock/page.tsx`, `components/checkout/receipt-overlay.tsx`, `components/dashboard/dashboard-header.tsx` — all out of 3.2's scope, each gets wired in 3.3. Delete this file only once every one of those stops referencing it, not before

### 3.3 Wire Each Module (real TanStack Query hooks replacing mock ones)

**Note (2026-07):** only Products and Transactions have a mock hook already (`hooks/use-products.ts`, `hooks/use-transactions.ts`, built in Part 1.5 as the pattern) — and neither is wired into its page yet, they still read `MOCK_X` via `useState` directly. Categories/Users/Stock Adjustments/Dashboard have **no hook file at all** yet (deferred on purpose, see the 1.5 scope note). So "replace the mock hook body" below means two different amounts of work: for Products/Transactions, swap the `queryFn` to a real `lib/api-client.ts` call _and_ rewire the page off `useState(MOCK_X)` onto the hook; for the other four, write the hook from scratch (real API call from day one) _and_ wire the page — there's no mock version to delete first.

For each module below: end up with a real `useQuery`/`useMutation` call against `lib/api-client.ts` (no fake delay), the page actually consuming the hook (not `useState(MOCK_X)`), and confirm the UI states (loading/error/empty) still behave correctly with real network conditions.

- [x] **Products** — checked `app/(staff)/products/page.tsx`: bigger rewrite than "swap the hook body". Today, `MOCK_PRODUCTS` is loaded whole and **filtered/paginated entirely client-side** (`.filter()` + `.slice()` in the page itself); the real `searchProducts` service does that server-side (`search`/`categoryId`/`page` query params), so filtering has to move from local JS logic into query params that trigger a refetch. `isAdmin` currently reads `mockCurrentUser.role` — replace with `useCurrentUser()` (built in 3.2). The category filter dropdown currently derives its options from `products.map(p => p.category)` (a name string, from the old mock shape) — the real `Product` type only has `categoryId` (a UUID), so the dropdown needs actual category data (name + id) from `GET /api/v1/categories`, meaning this page has a hard dependency on the **Categories** module below even though Categories is listed after it. Build `useCategories()` (or at least its list query) alongside this, out of order if needed, rather than blocking Products on strict list order. **`tsc` confirms this actually touches 5 files, not just the page** — logged them all below rather than discovering them one at a time
  - [x] `hooks/use-products.ts` — rewrite `useProducts(params: { search?, categoryId?, page? })`: builds a query string from `params` via `URLSearchParams`, `queryKey: ["products", params]` (params included so different filter/page combos cache separately, not overwrite each other), `queryFn` calls `apiGet<ProductSearchResult>`. **Caught while building:** first draft was typo'd `userProducts` instead of `useProducts` — wouldn't have been recognized as a hook (breaks the `use`-prefix convention React relies on) and the name wouldn't have matched any import elsewhere
  - [x] `hooks/use-categories.ts` (new file, pulled forward from the Categories module below) — `useCategories()`, no params needed (`GET /api/v1/categories` takes no query filters), simpler than `use-products.ts` — no `URLSearchParams` needed
  - [x] `components/products/products-table.tsx` — mechanical renames done: `stock` → `stockQuantity`, `price.toFixed(...)` → `Number(price).toFixed(...)`. Added a new `categoryNameById: Record<string, string>` prop (category name isn't on the product row anymore, just `categoryId` — this map is built once in `page.tsx` from `useCategories()` and passed down, instead of every row doing its own lookup)
  - [x] `components/products/delete-product-dialog.tsx` and `components/products/product-detail-sheet.tsx` — same renames done, `tsc` clean. `txnCount` (didn't exist on the real `Product` type — the soft-vs-hard delete decision is server-side now, per `deleteProduct` from Part 2.6) removed from both: the delete dialog's copy is now a generic "if it has sales history it'll be soft-deleted, otherwise removed" instead of asserting which one will happen; the detail sheet's "SOLD IN" row was dropped entirely rather than faked. `ProductDetailSheet` also got the same new `categoryNameById` prop as the table
- [x] `components/products/product-form-dialog.tsx` — done. `lib/validators/product.ts`'s `productSchema`: `category: z.string()` → `categoryId: z.string()` (`price`/`stock` deliberately stay strings — HTML inputs always produce strings; conversion to the backend's real shape happens later, in `page.tsx`'s submit handler). Component: `categories` prop type `string[]` → `Category[]` (both `ProductForm` and `ProductFormDialog`), `defaultValues` updated (`categoryId`, `stock: String(product.stockQuantity)`, `barcode: product.barcode ?? ""` since it can now be `null`), the category `<Controller>` renamed to `categoryId` with `SelectItem` now using `cat.id` as the stored value and `cat.name` as the displayed label (previously the same string did both jobs) — `tsc` clean
  - [x] `app/(staff)/products/page.tsx` fully rewired — `isAdmin` from `useCurrentUser()`, `categories`/`categoryNameById` from `useCategories()`, `useProducts({ search, categoryId, page })` replaces `useState(MOCK_PRODUCTS)` + local `.filter()`/`.slice()` entirely (pagination/total/current page now derived from the server response, not computed client-side). First real use of **`useMutation`** in the project (`createMutation`/`updateMutation`/`deleteMutation`, each closing its dialog in `onSuccess` — no cache invalidation yet, deliberately 3.4's job, see 3.4's own checklist item for this module). `handleFormSubmit` now translates the form's `ProductFormValues` shape into the backend body shape (`stockQuantity: Number(values.stock)`, etc.) — the boundary between the two schema layers. `isEmpty`/`hasNoResults` re-derived from `hasActiveFilter` (search/category filter engaged) + `data.total`, since the old two-array (`products` vs `filtered`) distinction doesn't exist anymore now that filtering is server-side. Reload button now calls `refetch()`. **Caught while building:** `categoryFilter`'s state type had to drop `| null` (`Select`'s `onValueChange` type allows `string | null` even though this `Select` never actually clears to null — guarded with `if (value)` rather than widening the state type back to accept `null`)
  - [x] `lib/mock-products.ts` — checked, still referenced by `checkout/page.tsx`, `dashboard/page.tsx`, `stock/page.tsx` (all still-unwired modules) — not deleted yet, same situation as `mock-current-user.ts` in 3.2
  - [x] **Bug found via manual testing, fixed (2026-08):** the category filter's trigger showed the raw UUID instead of the category name after selecting. Root cause: `@base-ui/react/select`'s `Select.Value` shows the raw `value` unless `Select.Root` is given an `items` prop (a `value → label` map) — previously invisible because `value` and the displayed label were the same string (the category name); once `value` became the `categoryId` (UUID) and the label stayed the name, the mismatch became visible. Fixed by passing `items={{ "All categories": "All categories", ...categoryNameById }}` to the filter's `<Select>` in `page.tsx`, and `items={categoryNameById}` (a new local map, same `Object.fromEntries` pattern) to the category `<Select>` inside `product-form-dialog.tsx` — same underlying bug existed in both places since they share the identical `Select`/`SelectItem` pattern, just hadn't been noticed in the form dialog yet
  - [x] **Added beyond original scope, user-requested (2026-08):** success/error toast feedback on create/update/delete, via `sonner` — already installed and scaffolded (`components/ui/sonner.tsx`, `<Toaster />` already mounted in `app/layout.tsx` from Part 1, just never actually used until now). No new component needed — reusable as-is across every module. Also added `onError` handlers to all 3 mutations (previously a failed mutation gave **zero** user feedback — dialog just sat there silently); `error instanceof ApiError ? error.message : "generic fallback"` distinguishes a real API error (backend's own message, e.g. "Barcode is already in use.") from a lower-level failure (network error, etc. — those don't get a user-friendly `.message`). This pattern repeats for every module's mutations from here on, not just Products
- [x] **Categories** — checked `app/(staff)/categories/page.tsx` and its 3 dialogs. Same `productCount`/`sampleProducts`-doesn't-exist-on-the-real-type problem as Products' `txnCount`, but this time it's **not** safe to just drop the fields — `PRD.md` §3 explicitly requires "As an Admin, I want to view a category's detail so that I can see which products belong to it," so the detail view specifically needs real product-membership data, not a simplification. The card grid's summary (count + sample names) is a different story — nothing in the PRD requires every card to show a live count, and computing that for every category on the grid would mean an extra query per card (or a new backend aggregate endpoint that doesn't exist in `TECH_SPEC.md`'s file layout) — dropping that part is the right call, same reasoning as `txnCount`
  - [x] `hooks/use-categories.ts` — already built in the Products module (pulled forward), nothing more needed here
  - [x] `components/categories/category-form-dialog.tsx` — confirmed no changes needed. Its client-side duplicate-name pre-check (`existingNames: string[]`) still works fine fed from real `categories` data — it's a UX nicety (instant feedback before a round-trip), not a replacement for the backend's own uniqueness check (`createCategory`/`updateCategory` still 409 on duplicates regardless)
  - [x] `components/categories/delete-category-dialog.tsx` — `category.productCount > 0` (didn't exist on the real type) replaced with generic confirmation copy (can't assert the outcome upfront anymore, same principle as `DeleteProductDialog`) plus a new optional `error?: string` prop — shown inline when the delete attempt actually gets rejected server-side (`CONFLICT`), instead of pre-computing the outcome
  - [x] `components/categories/category-detail-sheet.tsx` — `category.productCount`/`category.sampleProducts` replaced with a real, on-demand fetch: `useProducts({ categoryId: category?.id })` called inside this component itself (only fires while the sheet is actually rendered, not eagerly for every card in the grid) — this is what actually satisfies the PRD requirement above
  - [x] `app/(staff)/categories/page.tsx` — `useState(MOCK_CATEGORIES)` → `useCategories()`; grid cards simplified to just name + icon (dropped count/sample/"Empty" badge — no cheap way to compute that per-card without N+1 queries, and PRD doesn't require it at the grid level); create/update/delete as `useMutation`, matching the Products pattern including `sonner` toasts and `onError` handling — except `deleteMutation`'s `onError` sets `deleteError` state (shown inside `DeleteCategoryDialog`) instead of only a toast, since the user needs the dialog to stay open with the reason visible, not just a passing notification
  - [x] `lib/mock-categories.ts` — deleted. Unlike `mock-products.ts`/`mock-current-user.ts`, nothing else in the codebase referenced it anymore once this page was rewired
  - [x] **Bug found via manual testing, fixed (2026-08):** unlike `products/page.tsx`, `categories/page.tsx` never had an `isAdmin` check at all — not even back in Part 1. A cashier logging in could see and click "New category" plus every card's edit/delete icons; clicking them correctly got rejected by the backend (`403 FORBIDDEN`, now visible as a red toast thanks to the `onError` handlers just added) but the UI never should have offered those actions in the first place. Fixed: added `useCurrentUser()` + `isAdmin` to the page, gated the header's "New category" button, the empty-state's CTA, and each card's edit/delete icon buttons behind `isAdmin &&`. `CategoryDetailSheet` gained a new required `isAdmin` prop, gating its `SheetFooter` (Rename/Delete) the same way `ProductDetailSheet` already did — this page just never had the equivalent check to begin with
  - [x] **Follow-up, user-requested (2026-08): restored the grid card's product count.** Needed changes on both sides — the count didn't exist anywhere in the API response before this:
    - [x] **Backend** — `lib/services/category-service.ts`'s `listCategories()`: `LEFT JOIN` against `products` (`eq(products.categoryId, categories.id)`, filtered to `eq(products.isActive, true)` — count only catalog-visible products, same visibility rule `searchProducts` already uses) + `count(products.id)` + `GROUP BY` on the category columns, same shape as `getBestSellers`'s join+group+count in `dashboard-service.ts`. `count()` comes back as a string from the driver (same as every other aggregate used so far) — mapped through `Number(...)` after the query, not left as a lying `number`-typed string
    - [x] `lib/types.ts` — new `CategoryWithProductCount = Category & { productCount: number }` (same pattern as `TransactionDetail`) — only the **list** endpoint gets this enriched shape; `getCategoryById`/`updateCategory`/`deleteCategory`'s plain-row responses don't carry a fake count
    - [x] `hooks/use-categories.ts` — return type now `CategoryWithProductCount[]`
    - [x] `app/(staff)/categories/page.tsx` — count line + "Empty" `Badge` restored on each card, driven by the real `productCount`. Deliberately did **not** restore the mock's `sampleProducts` mini-preview (a few example names) on the card itself — that would need a second aggregate (e.g. Postgres `array_agg`) just for a grid decoration, real names are already one click away in the detail sheet
    - [x] Re-tested via `curl` — `GET /api/v1/categories` now returns `productCount: 3` for Espresso, `productCount: 1` for Pastry, matching the seeded/created products
- [x] **Users** — checked `app/(staff)/users/page.tsx` and its 4 dialogs. Several mismatches, plus one dialog that was pure fiction (never called the API at all):
  - **`role` capitalization:** mock uses `"Admin" | "Cashier"` everywhere (badges, filter dropdown, the role-toggle buttons in the form) — real `roleEnum` is lowercase (`"admin" | "cashier"`). Decision: kept the UI-facing labels/values as `"Admin"/"Cashier"` (no point relabeling buttons users already know), converted to lowercase **only at the API boundary** in `page.tsx`'s submit handler (`values.role.toLowerCase()`), same "convert at the seam" principle as Products' `stockQuantity: Number(values.stock)` — **plus a second conversion point caught while building**: `user-form-dialog.tsx`'s `defaultValues` needed the reverse conversion (`user.role === "admin" ? "Admin" : "Cashier"`) so the edit form's role toggle actually highlights the current role correctly, not just the submit direction
  - **`active: boolean` → `isActive: boolean`** (renamed, not just a type change — every reference updated)
  - **`joined: string` → `createdAt: Date`** — mock had it pre-formatted as a display string; real field is an actual `Date`. **New gotcha hit while building:** `user.createdAt` is typed `Date` but arrives over JSON as a plain ISO string at runtime (`apiFetch`'s `as T` is a type-level claim, not a runtime guarantee) — calling `.toLocaleDateString()` directly on it would crash; wrapped in `new Date(user.createdAt)` first. Applies to every other `createdAt` field in the app too, just hadn't been rendered anywhere until now
  - **`last: string` ("last active") — doesn't exist anywhere in the real schema, dropped entirely.** No last-login tracking exists in the backend (no column, no update-on-login logic) — out of scope for wiring an existing UI to an existing API, so the "LAST ACTIVE" row was removed from the table and detail sheet, not faked
  - **`ResetPasswordDialog` was pure fiction — now wired for real**, to `POST /api/v1/users/{id}/reset-password`. Password still generated client-side (unchanged), but now actually sent to the server via `useMutation`; `done` state now comes from `resetMutation.isSuccess` (first real use of a mutation's success flag, not just `isLoading`/`isError`) instead of a local boolean that only ever reflected the client-side illusion
  - **Follow-up, user-requested (2026-08):** the dialog only offered "Generate password" with no way for the admin to type their own — inconsistent with the "Add staff" form's password field, which already lets you type manually **or** click Generate to fill it. Added a real `<Input>` bound to the password state (editable directly), kept the "Generate" button as a fill-the-input helper rather than an auto-submit action, and disabled the "Reset password" button below 6 characters (matching the backend's own `resetPasswordBodySchema` minimum) with an inline hint. The state variable was renamed `tempPassword` → `password` since it's no longer just a generated placeholder — it's a real editable field now
  - **No `isAdmin` gating added** — unlike Products/Categories, `/users` is already admin-only at the route level (`ROUTE_ROLES["/users"]`, enforced by `middleware.ts`), so a cashier can never reach this page; an in-page check would be redundant here, not a fix for a real gap
  - [x] `hooks/use-users.ts` (new file) — `useUsers()`, no params, same simplicity as `use-categories.ts`
  - [x] `components/users/user-form-dialog.tsx`, `components/users/deactivate-user-dialog.tsx`, `components/users/user-detail-sheet.tsx` — all field renames applied (`isActive`, `createdAt` formatting, lowercase `role` comparisons)
  - [x] `components/users/reset-password-dialog.tsx` — real mutation wired, see above
  - [x] `app/(staff)/users/page.tsx` — `useState(MOCK_USERS)` → `useUsers()`; `roleFilter`/search stay **client-side** (unlike Products) since `listUsers()` takes no query params at all — filtering the full list in JS is correct here, not a shortcut. Create/update/deactivate as `useMutation` + `sonner` toasts + `onError`. `setActiveMutation` handles both activate and reactivate with one mutation (just a different `isActive` value); its `onSuccess` reads back the `variables` TanStack Query passes (the object originally given to `.mutate(...)`) to pick the right toast text, rather than relying on component state, which had already been cleared
  - [x] `lib/mock-users.ts` — deleted, nothing else referenced it
  - [x] Re-tested via `curl`: `GET /users` returns the correct shape (no `passwordHash`), reset-password actually changes the stored hash — confirmed end-to-end by logging in with the newly reset password successfully
- [x] **POS/Checkout** — checked `app/(staff)/checkout/page.tsx` and all 6 supporting files (`stores/cart-store.ts`, `checkout-header.tsx`, `product-grid.tsx`, `cart-panel.tsx`, `checkout-dialog.tsx`, `receipt-overlay.tsx`). Biggest module so far — this touches almost every file in the folder, plus two small **backend** additions. Key decisions:
  - **Backend needs a real addition, not just wiring:** `GET /api/v1/products` never forwarded a `pageSize` query param (the route handler only reads `search`/`categoryId`/`page`, even though `searchProducts` itself already accepts `pageSize`). The POS screen needs the **whole catalog at once** (no admin-style pagination — cashiers scroll one flat grid), so this has to be fixed: `app/api/v1/products/route.ts`'s `GET` reads `pageSize` from `searchParams` too, and `hooks/use-products.ts` gains a `pageSize` param. Checkout calls `useProducts({ pageSize: 1000 })` — one big fetch, no `search`/`categoryId` sent to the server at all
  - **Filtering/sorting stays entirely client-side**, same reasoning as Users: once the whole catalog is fetched once, POS needs instant, no-lag filtering while typing or tapping a category pill — a network round-trip per keystroke (like the Products admin page does) would make the POS screen feel laggy, which matters more here than anywhere else in the app
  - **"Sort: Popular" is dropped** — it sorted by `txnCount`, which doesn't exist on the real `Product` (same reasoning as everywhere else `txnCount` showed up). Default sort becomes `"Name A–Z"` instead
  - **Every `product.price` arithmetic needs `Number(...)` first** — `price` is a string now; JS's `*`/`-` operators happen to coerce numeric strings at runtime, but TypeScript won't compile `string * number`, so every cart/receipt/sort calculation needs an explicit conversion, not a shortcut
  - **`Receipt` type finally gets defined** in `lib/types.ts` (left undecided back in 3.1 on purpose) — kept it numeric-friendly (`lines: {name, qty, price: number}[]`, `subtotal`/`paid`/`change: number`) so `receipt-overlay.tsx`'s existing `formatUSD(number)` calls don't need to change at all; only its `mockCurrentUser` import needs replacing with `useCurrentUser()`
  - **`checkout()`'s response doesn't include line items** (it returns the bare `transactions` row, not `{...transaction, items}`) — the receipt's `lines` are built from the **cart itself** (already known client-side before submitting), not re-fetched from the server. `subtotal`/`paid`/`change` **are** taken from the real transaction response (`totalAmount`/`amountReceived`/`changeAmount`), not recomputed locally — server is authoritative for the money figures
  - **New pattern: per-call `mutate(variables, { onSuccess })` instead of the mutation-level `onSuccess`.** Every other module used the mutation-level `onSuccess` (defined once inside `useMutation({...})`) because it didn't need data from the exact moment `.mutate()` was called. Here it does — the receipt needs a snapshot of the cart's line items *as they were at submit time*, so that snapshot is built right before calling `.mutate(body, { onSuccess: (transaction) => {...} })`, with the snapshot captured in the surrounding closure. The mutation-level config still keeps a general `onError` (toast), since error handling doesn't need that snapshot
  - **"Wire the `INSUFFICIENT_STOCK` error" resolves via the same `toast.error(error instanceof ApiError ? error.message : ...)` pattern as every other module** — no bespoke inline banner needed; the backend's real message (e.g. "Not enough stock for Cappuccino.") surfaces directly. The checkout dialog isn't auto-closed on error, so the cashier sees the toast and can adjust the cart without losing their place
  - **Bonus, noticed while touching this code:** the search input's placeholder already promised "Search products or scan barcode…", but the old client-side filter only ever matched `p.name` — barcode was never actually searched despite the label. Since real `barcode` data exists now, added it to the match condition while already rewriting this filter (one extra `||` clause, not a separate task)
  - [x] **Backend:** `app/api/v1/products/route.ts`'s `GET` — now forwards `pageSize` from `searchParams` to `searchProducts`. Re-tested via `curl`: `?pageSize=1` correctly returns exactly 1 item with `total: 4` (the real full count) — proves the limit and the count are independent, not accidentally coupled
  - [x] `hooks/use-products.ts` — accepts an optional `pageSize` param, same `URLSearchParams.set(...)` pattern as `page`/`search`/`categoryId`
  - [x] `stores/cart-store.ts` — `Record<number, number>`/`number[]`/every `productId: number` → `string` (real product ids are UUIDs) — pure ID-type change, `tsc` clean, no logic touched
  - [x] `lib/types.ts` — `Receipt` type added, deliberately numeric (not string like the DB-derived types) since it's a display-only shape assembled client-side, not something sent to/stored by the API — the money rule applies to data being persisted/transmitted, not a value already converted once for `formatUSD()`
  - [x] `components/checkout/checkout-header.tsx` — `categories: string[]` → real `Category[]`, combined with an `"All"` pseudo-entry into a local `pillItems` array (`{id, name}[]`) so each pill has both its filter value (id) and display label (name); per-pill counts now filter by `p.categoryId === item.id`. `sort` prop and its `<Select>` both dropped `| null` — the null-guard (`value && onSortChange(value)`) lives inside this component now, since the `<Select>` itself is here, not in `page.tsx`. Dropped the "Sort: Popular" `<SelectItem>` entirely
  - [x] `components/checkout/product-grid.tsx` — `quantities`/`onAddItem` id types `number`→`string`; `product.stock`→`product.stockQuantity`; `formatUSD(product.price)`→`formatUSD(Number(product.price))`
  - [x] `components/checkout/cart-panel.tsx` — `product.stock`→`product.stockQuantity`; `onIncrement`/`onDecrement` id types `number`→`string`; every `product.price` arithmetic wrapped in `Number(...)` before multiplying, not after (`Number(product.price) * qty`, not `Number(product.price * qty)` — the latter fails to compile since `price * qty` itself is the invalid operation)
  - [x] `components/checkout/checkout-dialog.tsx` — **no changes needed**, confirmed: every prop it receives (`subtotal`, `quickCashValues`, `change`) is already plain `number`, computed correctly upstream in `page.tsx`
  - [x] `components/checkout/receipt-overlay.tsx` — swapped `mockCurrentUser` for `useCurrentUser()`, called **before** the `if (!receipt) return null;` early return (Rules of Hooks — same ordering already used correctly in `staff-user-panel.tsx`, just made explicit here). Nothing else changed since `Receipt` was deliberately typed to match what this component already expected
  - [x] `app/(staff)/checkout/page.tsx` — full rewire done: `useProducts({ pageSize: 1000 })` + `useCategories()` replace `useState(MOCK_PRODUCTS)`; client-side filter/sort updated for `categoryId`/`Number(price)`/dropped Popular sort/added barcode match; `checkoutMutation` (`POST /transactions`) replaces the local `setProducts`-mutates-stock fake, using the per-call `mutate(body, { onSuccess })` pattern so the receipt gets an accurate cart snapshot; `getReceiptTimeLabel` now takes the real `transaction.createdAt` instead of always using "now". `tsc` clean across the whole module
  - [x] `lib/mock-products.ts` — checked, still referenced by `stock/page.tsx` and `dashboard/page.tsx` (not yet wired) — not deleted yet
  - [ ] **Known temporary gap, same as every other module (2026-08):** after a successful sale, the product grid's stock numbers **don't** auto-update — no cache invalidation yet, that's still 3.4's job. Unlike Products/Categories though, this one actually matters more mid-shift (a cashier could oversell an item that just ran out) — flagged here explicitly so it isn't missed when prioritizing 3.4's checklist, but not being fixed early; the established rule (no invalidation until 3.4) still holds
  - [x] **Manually tested end-to-end (2026-08), all 21 scenarios passed:** real product/category data in the grid, per-category pill counts, category filtering, name search, barcode search, all 3 sort options, add-to-cart/increment/quantity-sync, over-stock line highlighting + disabled Charge button, Clear cart, quick-cash buttons, invalid/valid received-amount states, full successful checkout (receipt shows correct ref/time/cashier/lines/totals, database confirms the transaction + stock decrement), New Sale reset, server-side stock-conflict error surfaces as a toast without closing the dialog or clearing the cart, both admin and cashier can complete a sale
  - [x] **Follow-up, user-requested (2026-08): the receipt's "Print" button now actually works.** Was a dead button — `receipt-overlay.tsx`'s `<Button>` had no `onClick` at all, same "looks real, does nothing" pattern as the old `ResetPasswordDialog`. Implemented:
    - `onClick={() => window.print()}` — the browser's native print dialog, no custom print-preview UI
    - `app/globals.css` gained its first print-only stylesheet (project had none): `@media print { body * { visibility: hidden; } .receipt-print-area, .receipt-print-area * { visibility: visible; } .receipt-print-area { position: absolute; top: 0; left: 0; width: 100%; } }` — isolates printing to just the receipt card, hiding the sidebar/product grid/dark backdrop behind it. Uses `visibility` rather than `display` deliberately: a descendant's `visibility: visible` can override an ancestor's `visibility: hidden` (unlike `display: none`, which removes the element from the render tree with no way for a child to opt back in) — that's the mechanism that makes this whole trick work
    - `receipt-print-area` class added to the receipt card in `receipt-overlay.tsx`, plus `print:shadow-none print:rounded-none` (Tailwind's print variant) so the card prints as a clean rectangle instead of showing a drop shadow on paper
    - The Print/New Sale button row got `print:hidden` (Tailwind) — even though it's inside `.receipt-print-area` (so the global stylesheet would otherwise make it visible), `display: none` wins over an inherited `visibility: visible`, so it still correctly disappears when printing
    - **Scope decision:** targets a normal printer via the browser's print dialog (Letter/A4) — thermal receipt-printer paper widths (58mm/80mm, needs `@page` sizing + a different layout) are a separate, bigger feature, not built here
    - **Bug found via user testing, fixed (2026-08):** first version printed with every background color stripped out (the green checkmark circle, the "Cash received/Change" shaded box) — browsers default to *not* printing background colors/images at all (a "conserve ink" default), regardless of what's on screen. Fixed by adding `print-color-adjust: exact` + `-webkit-print-color-adjust: exact` (older Safari/Chrome still need the prefixed form) to `.receipt-print-area *` in the print stylesheet, telling the browser to actually honor the page's colors when printing
    - **Known, unfixable from the page itself:** the printed page also showed Chrome's own header/footer (date, page title, URL, page number) — that's the browser's print chrome, not anything `receipt-overlay.tsx`/`globals.css` renders. A web page has no CSS/JS lever to force this off; the user has to disable "Headers and footers" in the browser's print dialog (under "More settings") themselves
- [ ] **Transaction History** — checked `app/(staff)/transactions/page.tsx` and all 5 supporting files (`transaction-stats.tsx`, `transaction-filters.tsx`, `transactions-table.tsx`, `transaction-detail-sheet.tsx`, `void-transaction-dialog.tsx`), plus `PRD.md` §6 and the existing `lib/services/transaction-service.ts`/routes from Part 2.7. Second-biggest module after Checkout, with a real spec conflict to resolve first.
  - **PRD conflict, resolved by user decision (2026-08):** `PRD.md` §6 states "Cashiers can view all transactions (read-only) but cannot void them" — but `listTransactions` (built in 2.7) deliberately auto-scopes a cashier caller to their own `cashierId` only, ignoring any `cashierId` they try to pass. **Decision: keep the existing restriction** (cashiers see only their own sales) — more security-conscious (staff don't see each other's sales patterns/performance), and this was already a deliberate call made in 2.7, not an oversight. `PRD.md` is now knowingly out of sync with the implementation on this one point — noted here rather than silently changing either side
  - **Backend needs two real additions, not just wiring:**
    - `app/api/v1/transactions/route.ts`'s `GET` never forwards `pageSize` (same class of bug as Products, before that fix) — the table's own `PAGE_SIZE = 8` would silently mismatch the backend's default of 20 per page, breaking the pagination math. Needs the same `pageSize` passthrough
    - **Date range filtering doesn't exist in `listTransactions` at all**, but `PRD.md` explicitly requires it ("filter the transaction list by date range"). The current UI's date chip is static decoration (`"Jul 7 – Jul 8, 2026"` hardcoded, a `ChevronDown` with no dropdown behind it) — this was already broken back in Part 1, not something Part 3 broke. `listTransactions` needs `from`/`to` params added (`gte`/`lte` on `createdAt`, same pattern as `getSalesSummary` in `dashboard-service.ts`), and the route needs to parse them from `searchParams` the same way `dashboard/summary/route.ts` already does (extract to a `fromParam`/`toParam` const first, avoid `new Date(string | null)` type errors)
  - **Real UI for the date filter, replacing the dead chip:** Part 1's own TODO note said this page's date control was meant to be "a 3-option segmented toggle (Today / 7 days / 30 days) + read-only resolved date-range chip" — that was never actually built here (only the chip shell exists, no toggle, no real dates behind it). Building it for real now, since Dashboard (later in 3.3) is documented to reuse this exact same pattern — this becomes the reference implementation for that, not a one-off
  - **Cashier name resolution needs `useUsers()`, but conditionally** — `Transaction`/`TransactionDetail` only carry `cashierId` (a UUID), not a name. Resolving names via `useUsers()` is fine for an admin (already has permission), but `GET /api/v1/users` is `requireAuth("admin")` — calling it unconditionally would fire a doomed `403` request for every cashier who opens this page. `hooks/use-users.ts` gains an `enabled` passthrough option (a genuinely new TanStack Query concept — conditionally skip a query), called as `useUsers({ enabled: isAdmin })`. For a cashier, `cashierNameById` stays empty and every row falls back to `currentUser?.name` — correct anyway, since (per the decision above) a cashier's own list is always just their own sales
  - **List response has no item data** (`Transaction`, unlike `TransactionDetail`, has no `items` array) — the old mock table's "Items" column (`getItemCount(t)`) can't be computed from the list response at all. Dropped the column entirely, same "drop rather than fake" precedent as `txnCount` elsewhere — not PRD-required at the list level anyway (PRD's item-level detail requirement is specifically for the *detail* view)
  - **Stats cards (`TransactionStats`: gross sales, avg ticket, etc.) are dropped, not wired.** They were computed by reducing over the *entire* mock array — but real data is paginated server-side, so only ~8 rows are ever loaded client-side; computing "gross sales" from just the current page would be silently wrong, not a simplification. Not a `PRD.md` requirement (that document's acceptance criteria cover list/filter/detail/void, no summary tiles) — a correct version would need its own backend aggregate (filtered by whatever cashier/status/date range is active), which is out of scope here. Left as a possible follow-up, same shape as the Categories product-count one, not built preemptively
  - **`lib/transaction-utils.ts` (`getTransactionTotal`/`getItemCount`) becomes unnecessary and gets deleted** — real `Transaction.totalAmount` and `TransactionItem.subtotal` are already precomputed server-side strings, no client-side recomputation needed
  - **Payment method hardcoded to "Cash" in the detail sheet**, not read from a `method` field — the real `transactions` table has no such column at all; this system only ever supported cash (`CheckoutDialog` was always "Cash received", never had a card path)
  - **Void, same "no invalidation yet" gap as everywhere else** — after a successful void, both the void dialog and the detail sheet close (matching "close what just completed" everywhere else), but reopening that same transaction's detail shortly after may still show stale pre-void data from cache (`staleTime` is 60s) until 3.4's invalidation work lands
  - [x] `hooks/use-users.ts` — added `options?: { enabled?: boolean }`, passed through to `useQuery`'s own `enabled`. `tsc` confirms the existing no-argument call site (Users page) still compiles unchanged
  - [x] **Backend:** `lib/services/transaction-service.ts`'s `listTransactions` — `from`/`to` params added (`gte`/`lte` on `createdAt`, same pattern as `getSalesSummary`). No default range (unlike Dashboard's "today" default) — an unbounded history view makes more sense here than a defaulted-to-today one
  - [x] **Backend:** `app/api/v1/transactions/route.ts`'s `GET` — parses `from`/`to`/`pageSize` from `searchParams` now, forwarded to `listTransactions`
  - [x] `hooks/use-transactions.ts` — full rewrite: `useTransactions(params: { cashierId?, status?, from?, to?, page?, pageSize? })` (list, `URLSearchParams` pattern) and a second export `useTransactionDetail(id: string | null)` using `enabled: id !== null` (only fetches once a row is actually selected, not eagerly)
  - [x] `lib/types.ts` — `TransactionSearchResult` added (`{ items: Transaction[]; total; page; pageSize }`), same shape as `ProductSearchResult`
  - [x] `components/transactions/transaction-filters.tsx` — real 3-option date-range segmented toggle replaces the dead chip; `cashierFilter` is a real user id (`"All cashiers"` sentinel + UUIDs), fed a `cashiers: User[]` prop from the page rather than the hardcoded `CASHIERS` mock array; `search` input dropped entirely — real transaction ids are UUIDs (not the short `TX-2041`-style mock ids), and `listTransactions` has no search-by-id support to wire it to anyway. **Also applied the `Select` `items` fix proactively** (the "shows raw id instead of label" bug from Products/Categories) — didn't have to be found again the hard way this time
  - [x] `components/transactions/transactions-table.tsx` — drop the "Items" column (see above); `t.time`→formatted `t.createdAt`; `t.cashier`→`cashierNameById[t.cashierId]` (prop from the page); `getTransactionTotal(t)`→`Number(t.totalAmount)` directly (already precomputed, no helper needed); receipt id shown as `t.id.slice(0, 8).toUpperCase()`, same short-ref convention as the Checkout receipt
  - [x] `components/transactions/transaction-detail-sheet.tsx` — takes `TransactionDetail` now (not the old mock `Transaction`), same renames plus items from `TransactionDetail.items` (real `productNameSnapshot`/`quantity`/`unitPriceSnapshot`/`subtotal` — `subtotal` already computed, no `item.qty * item.price` needed); "Payment · Cash" hardcoded (see above); "Voided by" resolves through the same `cashierNameById` map (an admin voids, so `voidedBy` is always an admin's id)
  - [x] `components/transactions/void-transaction-dialog.tsx` — **kept the mutation in `page.tsx`, not inside this component** (deviated from the original plan note here on purpose, for consistency with every other delete/deactivate dialog in the app — they're all "dumb" components that call an `onConfirm` prop, with the parent page owning the `useMutation`; moving the mutation into this one dialog would've been the odd one out). Takes `TransactionDetail` now (reusing the already-loaded detail data instead of re-finding the transaction in a list). `VOID_REASONS` (preset reason chips) moved in as a local constant — decorative UI text, not data, didn't belong in a phased-out mock file
  - [x] `components/transactions/transaction-stats.tsx` — **deleted**, not wired (see reasoning above)
  - [x] `app/(staff)/transactions/page.tsx` — full rewire: `useTransactions(...)` + `useTransactionDetail(detailId)` replace `useState(MOCK_TRANSACTIONS)`; `isAdmin` from `useCurrentUser()`; `cashierNameById` merges the admin-only `useUsers()` result with `{ [currentUser.userId]: currentUser.name }` so a cashier's own name always resolves even though their `useUsers()` call is disabled; void as a real mutation with `sonner` toasts + `onError`, matching every other module's pattern. `tsc` clean across the whole module
  - [x] `lib/mock-transactions.ts` and `lib/transaction-utils.ts` — both deleted, confirmed nothing else referenced either
  - [ ] **Manual testing — left for the user to run, not done here.** Cover at minimum: list loads with real data sorted newest-first, all 3 date ranges, status toggle (all/completed/voided), cashier filter (admin only, hidden for cashier), row click opens detail with correct items/totals/cashier name, void flow (admin only — button absent for cashier, reason required, stock restored per the already-tested 2.7 logic, both dialogs close on success), a cashier only ever sees their own transactions even via direct API/URL poking, voided rows show the strikethrough total + status badge in both list and detail
- [ ] **Stock Adjustments** — checked `app/(staff)/stock/page.tsx` and all 5 supporting files (`product-picker-list.tsx`, `product-header-card.tsx`, `adjustment-form.tsx`, `adjustment-history.tsx`, `confirm-adjustment-dialog.tsx`), against the real `lib/services/stock-adjustment-service.ts`/routes already built in Part 2.8.
  - **No `isAdmin` gating needed anywhere on this page** — same reasoning as Users: `/stock` is already admin-only at the route level (`ROUTE_ROLES["/stock"]`), a cashier can never reach it
  - **`StockAdjustment.result` (the resulting stock level shown per history row) doesn't exist in the real schema at all** — the `stock_adjustments` table only stores `quantity` changed, not a snapshot of the level it produced. Unlike `txnCount`/`method`/etc., there's no way to recompute this after the fact without replaying the entire adjustment history in order, which is real complexity for a display column. Dropped the "Result" column from `adjustment-history.tsx` — the live current stock is already visible on `ProductHeaderCard`, that's the number that matters day-to-day
  - **`entry.by` (admin name) needs `adminId` resolved via `useUsers()`** — but *unconditionally* this time, not gated behind an `enabled: isAdmin` like Transaction History's cashier-name lookup. Since this whole page is already admin-only (see above), whoever is viewing it is guaranteed to have permission to call `GET /api/v1/users` — no wasted-request problem to guard against here
  - **The hand-rolled toast (`useState<string | null>` + a `useEffect` `setTimeout` to auto-dismiss) gets deleted, replaced by `sonner`'s `toast.success(...)`** — that custom implementation predates `sonner` being wired into the app (Checkout's Print Receipt follow-up); no reason to keep two different toast mechanisms side by side now
  - **Product picker needs the full catalog, same as Checkout** — `useProducts({ pageSize: 1000 })`, filtered client-side by search + stock-status (all/low/out), for the same reason: this is a picker sidebar, not a paginated admin table, the admin needs to scroll through everything
  - **`selectedId` starts as `null`, no auto-select-first-product** — the mock version did `useState(products[0]?.id ?? null)`, which only worked because `MOCK_PRODUCTS` was available synchronously at render time. Real product data loads asynchronously, so that initializer would always capture `null` (nothing loaded yet) and never update itself later. Simplest correct fix: just start at `null` and let the existing "Select a product to adjust its stock" empty state handle it — no extra `useEffect` needed to fix what wasn't actually broken from a UX standpoint
  - **Client-side reject-not-clamp logic already matches the backend, no rewrite needed** — `handleOpenConfirm`'s existing check (`if (adjType === "decrease" && q > selected.stock) newErrors.qty = "Can't remove more than current stock..."`) already blocks an over-decrease **before** the confirm dialog ever opens, matching the backend's own reject decision from 2.8. The `Math.max(0, ...)` in the client-side `newLevel()` preview stays as a defensive fallback that should never actually be reached in practice, not something to "fix" — same conclusion reached when 2.8 was originally built
  - **Same "no invalidation yet" gap as everywhere else** — after a successful adjustment, neither the header card's current stock nor the history list auto-refreshes; the toast confirms success, but seeing the update requires reselecting the product (which refetches) or 3.4 landing
  - [x] `hooks/use-stock-adjustments.ts` (new file) — `useStockAdjustments(productId: string | null)`, `enabled: productId !== null` (same idea as `useTransactionDetail`). Create mutation stays inline in `page.tsx`, matching every other module's convention
  - [x] `components/stock/product-picker-list.tsx` — `product.stock`→`product.stockQuantity`, `product.category`→`categoryNameById[product.categoryId]`, `selectedId`/`onSelect` id types `number`→`string`
  - [x] `components/stock/product-header-card.tsx` — same renames
  - [x] `components/stock/adjustment-form.tsx` — `PRESET_REASONS` moved in as a local constant
  - [x] `components/stock/adjustment-history.tsx` — `entry.type`→`entry.adjustmentType`, `entry.qty`→`entry.quantity`, `entry.by`→`adminNameById` prop lookup, `entry.when`→formatted `entry.createdAt`, "Result" column dropped
  - [x] `components/stock/confirm-adjustment-dialog.tsx` — `product.stock`→`product.stockQuantity`
  - [x] `app/(staff)/stock/page.tsx` — full rewire done: `useProducts({ pageSize: 1000 })` + `useCategories()` replace `useState(MOCK_PRODUCTS)`; `useStockAdjustments(selectedId)` replaces `useState(MOCK_STOCK_HISTORY)`; create adjustment as a real `useMutation` with `sonner` toast + `onError`, replacing the hand-rolled toast and local array mutation; `adminNameById` from an unconditional `useUsers()`. `tsc` clean across the whole module
  - [x] `lib/mock-stock-history.ts` — deleted, nothing else referenced it (`PRESET_REASONS` moved out first)
  - [x] `lib/mock-products.ts` — checked again, still used by `dashboard/page.tsx` only — not deletable yet, as predicted
  - [ ] **Manual testing — left for the user to run, not done here.** Cover at minimum: picker list loads real products with correct stock/category/status dot, search + all/low/out filters, selecting a product loads its real history, increase and decrease both work, decrease past available stock is rejected (not clamped, matching 2.8's tested backend behavior) with the error shown before the confirm dialog ever opens, confirm dialog shows the right before→after numbers, toast appears on success, history table shows the right admin name and no "Result" column, a cashier can't reach `/stock` at all (redirected)
- [x] **Dashboard** — checked `app/(staff)/dashboard/page.tsx` and all 7 supporting files, against `PRD.md` §8 and the real `dashboard-service.ts`/routes built in 2.9. **Biggest gap between mock and real API of any module so far** — this isn't a rename pass, it's a real scope cut, flagged prominently rather than decided quietly:
  - **What the real backend actually provides:** `getSalesSummary({from, to})` → `{ totalSales, transactionCount }`, and `getBestSellers({from, to, limit})` → ranked `{ productId, productName, totalQuantity }`. That's it — two numbers and a ranked list. No period-over-period deltas, no time-bucketed series, no per-category revenue breakdown — none of that data exists anywhere in the backend
  - **What the mock UI (`lib/mock-dashboard.ts`) has that the real API can't produce:** every stat's delta pill ("↑ 12.4% vs yesterday" etc.), the "Items sold" stat (no endpoint sums quantity across an entire period, `getBestSellers` only ranks the top N products), the hourly/daily/weekly bar chart (`SalesChart` — needs a time-bucketed aggregate, doesn't exist), the "Sales by category" breakdown (`CategoryBreakdown` — needs revenue grouped by category, doesn't exist)
  - **Revised decision on deltas: buildable without new backend, restored.** Initially grouped the delta pills in with the other missing aggregates, but on a second look, `getSalesSummary({from, to})` already accepts arbitrary bounds — calling it a **second time** for the immediately-preceding period of the same length (`previousFrom = from - (to - from)`, `previousTo = from`) gives everything needed to compute `% change = (current - previous) / previous`, entirely client-side, no backend change. Restored delta pills for the 3 stats being kept (Total Sales, Transactions, Avg. Ticket) — costs one extra `useDashboardSummary` call, not a new endpoint
  - **Still cut, genuinely needs new backend work:** the "Items sold" stat (needs an aggregate that sums `transaction_items.quantity` across a whole period — doesn't exist), the hourly/daily bar chart (`SalesChart` — needs time-bucketed grouping), the "Sales by category" breakdown (`CategoryBreakdown` — needs revenue grouped by category). `PRD.md` §8 marks Dashboard **Should**-priority and deliberately "basic" — these three stay out of scope, not attempted halfway
  - **`SalesChart` and `CategoryBreakdown` are deleted entirely** — not simplified, not stubbed, removed. The Dashboard page will look sparser than the mock in those two spots — that's the direct, visible cost of this decision, not hidden
  - **Kept: period toggle, Total Sales, Transactions, Avg. Ticket (all three now with real delta pills), Best Sellers (real, minus the `category` sub-label — `getBestSellers` doesn't return a product's category, only `productId`/`productName`/`totalQuantity`), Low Stock** — this last one needs *no* new backend at all, already anticipated back in 2.9: it just reads the real product list, same as `low-stock.tsx` already did against `MOCK_PRODUCTS`
  - **"Items sold" stat is dropped** — not faked with a placeholder
  - **`hasNoSales` (drives the empty state) is redefined** from `snapshot.bars.every(b => b.value === 0)` (bars no longer exist) to simply `transactionCount === 0` — simpler and still exactly correct for "no sales in this period"
  - **No `isAdmin` gating needed** — same reasoning as Users/Stock: `/dashboard` is already admin-only at the route level
  - [x] `hooks/use-dashboard.ts` (new file) — two exports: `useDashboardSummary({ from?, to? })` and `useBestSellers({ from?, to?, limit? })`, both the same `URLSearchParams` pattern as every other list hook. No dedicated "delta" hook — `page.tsx` just calls `useDashboardSummary` twice (current + previous period)
  - [x] `lib/types.ts` — `DashboardPeriod`/`StatDelta` added
  - [x] `components/dashboard/dashboard-header.tsx` — `mockCurrentUser`→`useCurrentUser()`, `DashboardPeriod` import moved to `@/lib/types`
  - [x] `components/dashboard/stat-card.tsx` — just the `StatDelta` import path updated, signature otherwise unchanged
  - [x] `components/dashboard/stats-grid.tsx` — takes real `{ totalSales, transactionCount, avgTicket, deltas, deltaNote }` directly, 3 `StatCard`s (grid narrowed from 4 to 3 columns)
  - [x] `components/dashboard/best-sellers.tsx` — takes real `BestSeller[]`, `item.productName`/`item.totalQuantity`, keyed by `item.productId`, category sub-label removed
  - [x] `components/dashboard/low-stock.tsx` — `product.stock`→`product.stockQuantity` throughout, fed from `useProducts({ pageSize: 1000 })` in the page
  - [x] `components/dashboard/sales-chart.tsx` — **deleted**
  - [x] `components/dashboard/category-breakdown.tsx` — **deleted**
  - [x] `app/(staff)/dashboard/page.tsx` — full rewire done. **One layout deviation from the original plan:** with `SalesChart`/`CategoryBreakdown` gone, the two-column grid rows they anchored (`BestSellers` alongside a chart, `LowStock` alongside a breakdown) no longer made sense — an empty grid cell would've been worse than no grid. `BestSellers` and `LowStock` are now stacked as plain full-width sections instead, each keeping its own fixed inner width. Couldn't visually verify this in a browser, flagged for a look during manual testing
  - [x] `lib/mock-dashboard.ts` and `lib/mock-products.ts` — **both deleted**, confirmed nothing referenced either anymore. `mock-products.ts` was the longest-lived mock file (Products, Checkout, Stock, Dashboard all depended on it at various points) — its removal here closes out that whole line
  - [x] **Milestone: `rtk tsc` is clean across the entire project**, zero errors anywhere — the red state accepted back in 3.1 (when `lib/types.ts` was first rebuilt) is now fully resolved, every module in 3.3 is done
  - [ ] **Manual testing — left for the user to run, not done here.** Cover at minimum: each of the 3 periods shows correct real totals (cross-check against a `curl` or DBeaver query), delta pills show sensible up/down percentages (test by comparing today vs a day with no sales, to see the `previous === 0` fallback text), avg ticket math is right, best sellers list matches actual sales, empty state shows correctly for a period with zero transactions, low stock list matches real product stock levels, **visually confirm the stacked `BestSellers`/`LowStock` layout looks acceptable** (the one deviation above), a cashier can't reach `/dashboard` at all
  - [ ] **Manual testing — left for the user to run, not done here.** Cover at minimum: each of the 3 periods shows correct real totals (cross-check against a `curl` or DBeaver query), avg ticket math is right, best sellers list matches actual sales, empty state shows correctly for a period with zero transactions, low stock list matches real product stock levels, a cashier can't reach `/dashboard` at all

### 3.4 Cache Invalidation (this is where TanStack Query earns its place)

**Concept recap:** `invalidateQueries({ queryKey: [...] })` marks matching cached queries stale and triggers an automatic refetch for any that are currently mounted — different from `queryClient.removeQueries(...)` (already used once, in logout) which just deletes the cache entry outright, so the next mount starts from a loading state. By default `invalidateQueries` does **prefix matching**, not exact matching: `invalidateQueries({ queryKey: ["products"] })` matches every cached variant — `["products", { search: "latte" }]`, `["products", { pageSize: 1000 }]`, all of them — because they all *start with* `"products"`. This is why none of the wiring below needs to know the exact `params` any particular page used; invalidating the prefix is enough.

- [x] **Prep — query-key restructure:** `hooks/use-dashboard.ts`'s two prefixes unified to `["dashboard", "summary", params]` / `["dashboard", "best-sellers", params]`, so one `["dashboard"]` invalidation now covers both
- [x] **Products** — `createMutation`/`updateMutation`/`deleteMutation` each invalidate `["products"]` **and `["categories"]`** (live `productCount` per category would otherwise go stale)
- [x] **Categories** — `createMutation`/`updateMutation`/`deleteMutation` each invalidate `["categories"]` only
- [x] **Users** — `createMutation`/`updateMutation`/`setActiveMutation` each invalidate `["users"]`; `resetMutation` deliberately left alone (nothing displayed depends on it)
- [x] **POS/Checkout** — `checkoutMutation`'s per-call `onSuccess` invalidates `["products"]`, `["transactions"]`, `["dashboard"]`
- [x] **Transaction History** — `voidMutation` invalidates `["transactions"]`, `["products"]`, and `["dashboard"]` (stock restored + sales totals change)
- [x] **Stock Adjustments** — `createMutation` invalidates `["products"]` and `["stock-adjustments", selectedId]`
- [x] **Dashboard** — confirmed no mutations of its own, purely a target
- [x] Every `page.tsx` above calls `useQueryClient()` once near the top, not per-mutation. `tsc` clean across the whole project after wiring all 6 modules

### 3.5 Full End-to-End Manual QA

Walk through every flow in `PRD.md` Section "User Flows" for real, start to finish:

- [x] **Smoke test (Claude ran this via curl)** — login as admin + cashier ✓, list products/categories (live `productCount` confirmed) ✓, create a product ✓, checkout (stock 10→7) ✓, void (stock 7→10, dashboard summary excludes the voided sale) ✓, transaction history shows the voided entry ✓, delete product → soft-deleted (`isActive:false`, drops out of active list) ✓. All passed. No bugs found — the only surprise was `GET /products/{id}` has no handler (only `PUT`/`DELETE` exist), which turned out to be correct: nothing in the app ever fetches a single product by id, every screen reads from the already-loaded list
- [x] **UI test via Playwright (2026-08, via Claude)** — `@playwright/test` installed and configured (`playwright.config.ts`, `tests/`). `tests/global.setup.ts` logs in as the seeded admin via the API and provisions a dedicated `cashier_e2e` account (idempotent — reuses it on re-runs), saving storage state for both roles so individual specs start pre-authenticated where that isn't the thing under test. 8 spec files cover the flows below (15 tests total, all passing, verified stable across repeated runs). **Real bug found and fixed in the process** — not just a passing/failing report: both `app/(staff)/dashboard/page.tsx` and `app/(staff)/transactions/page.tsx` computed `new Date()` directly in the component body (`getPeriodBounds(period)` / `getDateRangeBounds(dateRange)`, unmemoized) and fed the result straight into a TanStack Query key. Every render produced a new `to` timestamp -> looked like a new query -> refetched -> the fetch success re-rendered the component -> new `to` again -- an infinite refetch loop (hundreds of requests/sec against `/api/v1/dashboard/summary` and `/api/v1/transactions`, confirmed via network log during a headless run). The curl smoke test above could never have caught this since it doesn't re-render anything. Fixed by wrapping both in `useMemo` keyed on `period`/`dateRange`. Run with `npm run test:e2e` (needs the DB + `npm run dev` already running; the config's `webServer` will reuse an already-running dev server on `:3000`, and `npx playwright install chromium` is needed once if browsers aren't installed yet).
- [x] Flow 1: Login (correct + incorrect credentials) — `tests/01-login.spec.ts`
- [x] Flow 2: Admin adds a new product, sees it appear in the POS product grid — `tests/02-add-product.spec.ts`
- [x] Flow 3: Cashier completes a full checkout, stock decrements, receipt shows correctly — `tests/03-checkout.spec.ts`
- [x] Flow 3 (error path): attempt checkout on an item with 0 stock, confirm it's blocked with a clear message — `tests/03-checkout.spec.ts` (second test — confirms the tile is disabled and shows "Out of stock", matching `PRD.md`'s "cannot be added to a cart" acceptance criterion)
- [x] Flow 4: Admin reviews dashboard for today, then changes date range — `tests/04-dashboard.spec.ts` (this is the test that surfaced the infinite-loop bug above)
- [x] Admin voids a transaction, confirms stock is restored and the product is sellable again — `tests/05-void-transaction.spec.ts`
- [x] Admin performs a manual stock adjustment, confirms it reflects immediately in the product list — `tests/06-stock-adjustment.spec.ts`
- [x] Cashier attempts to access an admin-only page (Users) directly by URL — confirm they're blocked — `tests/07-rbac.spec.ts` (also covers `/stock` and the sidebar not rendering the Users link)
- [x] Log out, confirm session is cleared and protected pages redirect to login — `tests/08-logout.spec.ts`

**Checkpoint:** at the end of Part 3, BrewPoint is a fully working, real, end-to-end application — this is your MVP.

---

## Part 4 — Launch (v1.0, REVISED 2026-09: VPS + Docker + CI/CD, not Vercel)

**Plan change (2026-09):** the original plan (below, kept for reference) was "deploy to Vercel" — near-zero config, but also near-zero learning value beyond clicking a button. Switched to **self-hosting on a VPS**, explicitly because the goal now includes learning real deployment + CI/CD mechanics, not just getting BrewPoint online. This is genuinely not a detour — `ROADMAP.md`'s own v1.3 phase ("Testing, Observability & CI/CD") already planned a GitHub Actions pipeline; this just does the deploy half of that pipeline for real (SSH to a server you control) instead of delegating it to Vercel's Git integration. A domain is already owned, so real HTTPS is in scope from the start, not an afterthought.

**What actually changes vs. the Vercel plan:** Vercel would have run the Next.js app *for* you (build, serverless functions, TLS, zero-downtime deploys — all invisible). On a VPS, every one of those is something you set up and can see failing when it fails — which is the point. Concretely this adds: a `Dockerfile` (package the app as a container), a reverse proxy for HTTPS (nginx or Caddy), and a CI/CD pipeline that does what Vercel's Git integration used to do (GitHub Actions SSHing into the server on every push to `main`).

<details>
<summary>Original Vercel-based plan (superseded, kept for history)</summary>

- Provision managed PostgreSQL (Neon, Supabase, or Railway Postgres)
- Deploy the Next.js app to Vercel (single deployment — UI and API together)
- Set production environment variables (`DATABASE_URL`, `JWT_SECRET`) in Vercel's project settings
- Run `drizzle-kit migrate` against the production database
- Re-run the seed script against the production database
- Write the project README
- Do one final full walkthrough of Part 3.5's QA list against the live deployed URL

</details>

### 4.1 Provision & harden the VPS

**Concept:** a VPS gives you a bare Ubuntu machine with nothing on it — no web server, no Docker, no firewall rules beyond the provider's defaults. Everything below is what "hosting" actually consists of once nothing is automated for you.

- [ ] Provision a VPS running **Ubuntu 22.04 or 24.04 LTS** (any provider — DigitalOcean, Hetzner, AWS Lightsail, Contabo, etc. all work identically from here on, since everything below is plain Linux/Docker, not provider-specific). 1 vCPU / 1-2GB RAM is enough for this app + Postgres at BrewPoint's scale
- [ ] **Harden SSH access before anything else touches the internet:** create a non-root user with `sudo`, copy your SSH public key to it (`ssh-copy-id`), then disable root SSH login and password auth entirely in `/etc/ssh/sshd_config` (`PermitRootLogin no`, `PasswordAuthentication no`) — a fresh VPS gets password-guessed by bots within minutes of going live, key-only auth closes that off completely
- [ ] Enable a firewall (`ufw`) allowing only `22` (SSH), `80` (HTTP, needed for Let's Encrypt's verification), and `443` (HTTPS) — deny everything else by default
- [ ] Install Docker Engine + the Docker Compose plugin on the VPS (Docker's official install script, not the outdated `apt` package)

### 4.2 Containerize the app

**Concept:** `docker-compose.yml` currently only runs Postgres for local dev — the app itself still runs via `npm run dev` on your own machine. Deploying means the *app* also needs to run as a container, on the VPS, not your laptop.

- [ ] Add `output: "standalone"` to `next.config.ts` — this makes `next build` emit a minimal, self-contained server bundle (only the files actually needed at runtime, node_modules pruned to production deps only) instead of requiring the full `node_modules` tree in the final image
- [ ] Write a multi-stage `Dockerfile`: a `deps` stage (`npm ci`), a `build` stage (copies deps + source, runs `next build`), and a slim final runtime stage (copies only the `standalone` output + `public/` + `.next/static`, runs `node server.js`) — multi-stage keeps the shipped image small since build tools/dev dependencies never make it into the final layer
- [ ] Add a `.dockerignore` (`node_modules`, `.next`, `.git`, `tests/`, `.env*`) so the build context isn't megabytes of stuff Docker doesn't need to see
- [ ] Extend `docker-compose.yml`: add an `app` service (`build: .`, depends on `db`), pointing its `DATABASE_URL` at the **Docker network hostname** `db` (e.g. `postgres://brewpoint:${DB_PASSWORD}@db:5432/brewpoint_db`) instead of `localhost` — inside Docker's internal network, containers reach each other by service name, not `localhost`
- [ ] **Real bug found while planning this (2026-09):** `docker-compose.yml`'s Postgres password (`123456990`) is hardcoded in plain text in a file that's checked into git, and `.env`'s `DATABASE_URL` reuses that exact same weak password. Fine for a throwaway local dev container; **not fine to carry to a VPS reachable from the internet.** Fix: move the password to a `.env` file (already gitignored — confirm this), reference it in `docker-compose.yml` as `${DB_PASSWORD}`, and generate a real random password for the VPS's `.env` — never reuse the dev one

### 4.3 Reverse proxy + HTTPS

**Concept:** the app container will listen on some internal port (e.g. `3000`) that isn't directly exposed to the internet. A reverse proxy sits in front of it, terminates HTTPS (so browsers see a valid certificate), and forwards plain HTTP traffic to the app container internally.

- [ ] **Recommended: Caddy**, not nginx+certbot, for the first deployment — Caddy gets free automatic HTTPS (via Let's Encrypt) from a ~5-line config file with zero manual certificate renewal setup, versus nginx needing a separate `certbot` install + a cron job + more config syntax to get the same result. nginx is more common in job postings, but Caddy is the better *first* reverse proxy to actually understand what a reverse proxy does, without fighting certificate config at the same time
- [ ] Point the domain's DNS **A record** at the VPS's IP address
- [ ] Add a `Caddyfile` (one block: `yourdomain.com { reverse_proxy app:3000 }`) and a `caddy` service to `docker-compose.yml`
- [ ] **Do this milestone before automating anything:** manually SSH into the VPS, `git clone` the repo, `docker compose up -d --build`, run `drizzle-kit migrate` + the seed script against the VPS's Postgres, and confirm the app loads over `https://yourdomain.com` in a browser. **Why manual first:** you can't tell whether a CI/CD pipeline is broken or whether the *deployment itself* is broken unless you've seen it work by hand at least once — automating a step you've never done manually just means the first failure you debug is two unknowns tangled together instead of one

### 4.4 CI/CD pipeline (GitHub Actions)

**Concept:** this replaces what "Vercel's Git integration" used to do invisibly — on every push to `main`, a pipeline should run checks, then deploy automatically if they pass, without you touching the VPS by hand again.

- [ ] Store 3 GitHub Actions secrets on the repo: the VPS's SSH private key, its IP/hostname, and the SSH username — never hardcode these into the workflow file
- [ ] Write `.github/workflows/deploy.yml`: on push to `main`, first run a **test gate** (`rtk tsc`, `npm run lint`, `npm run build`) — matching `ROADMAP.md` v1.3's "a PR can't be merged if checks fail" principle — then, only if that passes, SSH into the VPS and run `git pull && docker compose up -d --build`
- [ ] **Start with SSH-deploy (rebuild on the server), not a registry-based deploy.** The more "proper" version builds the Docker image in CI, pushes it to a registry (GitHub Container Registry is free), and has the VPS just `docker pull` a ready-made image — faster deploys, no build tools needed on the VPS. That's a good v2 once the simpler SSH-deploy version is understood end-to-end; skipping straight to it means debugging registry auth and the deploy mechanics at the same time
- [ ] Test the pipeline for real: make a trivial change (e.g. a copy tweak), push to `main`, watch the Action run in GitHub's UI, confirm the change is live on the domain afterward without touching the VPS manually

### 4.5 Production correctness & wrap-up

- [ ] Generate a real, strong `JWT_SECRET` for the VPS's `.env` — never reuse the local dev one
- [ ] Run the final Part 3.5 QA walkthrough (including the Playwright suite, pointed at the live domain via its `baseURL` config) against `https://yourdomain.com`, not localhost
- [ ] Run the Part 3's still-pending **manual concurrency test** against the live server too, if not already done locally — network latency on a real server changes the timing window, worth re-confirming there
- [ ] Write/update the project README with the actual deployed architecture (VPS + Docker + Caddy + GitHub Actions) — per `ROADMAP.md`'s own working principle #3, explain *why* each piece was chosen (e.g. why Caddy over nginx here), not just how to run it, since this doubles as portfolio documentation

**Once every box above is checked, MVP v1.0 is live — the rest of your journey continues in `ROADMAP.md` starting at v1.1 (indexing, caching, rate limiting), noting that the Redis/Upstash and CI/CD sections there will need a small reread since they were written assuming Vercel.**

---

## Part 5 — Small Post-MVP Enhancements (optional, picked from 2026-08 feature brainstorm)

Two small nice-to-haves, kept intentionally simple — not full scope notes like Part 2/3, since these are polish, not core MVP requirements.

- [ ] **Staff performance mini-list on Dashboard** — small list under/near Best Sellers showing each cashier's transaction count + total sales for the selected period. Data already exists (`transactions.cashierId`); add a `GROUP BY cashierId` aggregate to `dashboard-service.ts` (a new function, e.g. `getCashierPerformance({ from, to })`), a `hooks/use-dashboard.ts` query for it, and a small component to render it. Admin-only (page already gated at the route level).
- [ ] **Filter Transaction History by product** — let admin/cashier narrow the transaction list to only transactions containing a specific product. `listTransactions` in `transaction-service.ts` needs a join against `transaction_items` plus a new `productId` filter param; `transactions/route.ts` forwards it from `searchParams`; `transaction-filters.tsx` gets a product-picker `Select` (reuse the existing `useProducts()` hook for the option list, same `items` map pattern as the cashier filter).
